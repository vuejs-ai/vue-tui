# API foundation roadmap

> **Status:** completed unstamped execution ledger. F1 through F8 are Done, and Runtime closure R1 through R12 is Closed on local candidate implementation head `e31aa1a`. R13 through R17 retain their original classifications and are Non-blocking. No foundation is Active or Queued, and no successor is activated automatically. [api-design.md](./api-design.md) owns the rationale and selected shapes; this record owns the completed order and status.

## Why this exists

A durable design note is not an implemented capability. This ledger records which proposed contracts became complete so a future contributor does not mistake a historical candidate for public API or reopen a settled dependency without new evidence.

Foundation work is ordered by dependency and reuse, not by which feature was discussed most recently. At most one foundation may be Active at a time; currently none is Active or Queued.

If foundation work is explicitly reopened, the [autonomous iteration plan](./autonomous-iteration.md#current-live-objective) must name the same Active item. A completed checkpoint does not automatically activate another foundation.

An item belongs here when it changes a shared application contract, runtime ownership, interaction delivery, lifecycle, or testing foundation used by several scenarios. Component-catalog additions, application-specific behavior, and speculative renderer optimization do not belong here.

## Status rules

- **Active:** the at-most-one foundation currently being closed.
- **Queued:** ordered work whose dependencies or active predecessor are incomplete.
- **Parked:** a possible extension without evidence that it belongs on the current critical path.
- **Done:** the public contract, runtime behavior, implementation, types, tests, records, and required real-terminal evidence all agree. A design decision or prototype alone is not done.

Moving an item to Done requires, as applicable:

1. an accepted public contract with requested, effective, unavailable, cleanup, and error behavior;
2. an explicit target disposition for every affected current API: retain, replace, or remove it; experimental APIs need no alias, deprecation window, or compatibility shim, but code, types, docs, examples, and tests must agree on the result;
3. implementation through public package exports without exposing host, Yoga, cell-buffer, or terminal-controller internals;
4. template and TSX type tests, API export guards, focused behavior tests, and lifecycle tests;
5. coverage across inline, full-screen, non-TTY/static, screen-reader, deterministic-test, HMR, and teardown environments where the API can be called;
6. real-PTY verification for terminal modes, screen ownership, coordinates, input fallthrough, restoration, or other terminal-visible claims;
7. updated canonical records and `vp run ready` passing.

## Current checkpoint

**Completed Runtime foundation:** F1 through F8 are Done, and R1 through R12 are Closed on local candidate implementation head `e31aa1a`. R13 through R17 retain their original classifications and are explicitly Non-blocking. No foundation item is Active or Queued, and no successor is activated automatically. Final capacity, packed-consumer, focus, PTY, visual, repository, remote-state-boundary, and review evidence is recorded in [Runtime foundation closure](./runtime-foundation-closure.md#final-closure-evidence).

**Historical F8 checkpoint:** F8 first became Done at `3d7e197`. The root exposes one app-owned `useClipboard()` service configured by one custom or OSC 52 mount transport; `/fullscreen` exposes `useTextSelection(target, { isActive?, pointer? })` for exactly one top-level Text. Selection follows successful final-paint provenance, keeps one active range per app, exposes command-driven and pointer extension, and returns exact selected text through the separate clipboard service for caller-owned fallback. The exact completed contract is [Fullscreen text selection and clipboard](./fullscreen-selection-and-copy.md); final Runtime evidence is centralized in the closure record.

**Completed Runtime-closure checkpoint:** the `/inline` `Static` cutover, transactional committed-history acceptance, `Object.is`-identical committed prefix, coordinated-output result and ordered flow-control gate, Box-rooted `v-show`, fork/non-TTY proof, structural foreground reset, exact package surfaces, family-internal adapters, fixed capacity workloads, and final focus-generation correction all agree. A host-only rendered reconciliation reuses an accepted focus generation only when no logical mutation is pending and the complete route, rendered-target order, and sequential-target order are identical; any semantic change replaces it. F3 still fails only the stale selected path closed, while independent application-global handlers for the same fact remain unaffected.

**Completed checkpoint:** F1.2 — the maintainer accepted one canonical `createApp`, optional `mode: "inline" | "fullscreen"` with Inline as the omission default, fail-fast invalid and removed options before terminal mutation, and the pinned Ink v7.0.4 non-TTY output policy. These are accepted unstamped decisions; no VOUCHED stamp was added. F1.2 changed the target records and added current-behavior evidence, but did not implement the mount replacement or complete F1.

The accepted [rendering-mode and host behavior matrix](./rendering-mode-matrix.md) contains the exhaustive input-validation and host-resolution tables. Normal non-TTY output defaults to committed `/inline` Static bytes plus the latest dynamic frame at clean teardown, while an explicit live-update override may emit relative or linear stream updates without acquiring a terminal mode. Input capability remains independent from this stdout policy. F1.4 implemented the mount replacement and live resolver, F1.5 completed the deterministic-test and string-host authority, F1.6 completed the bounded non-destructive Inline ownership contract, F1.7 completed lifecycle behavior, and F1.8 published and verified the shared readonly session surface. F1 is Done.

**Completed checkpoint:** F1.3 — selected the unstamped [`useRenderSession()` proposal](./render-session.md): one injected internal service with a readonly reactive public projection, discriminated mode resolution, orthogonal output destination/dynamic-update/presentation, separate terminal/layout dimensions, a derived `useLayoutSize()`, semantic structural capabilities, explicit host/lifetime behavior, and direct dispositions for overlapping current hooks. Pinned Ink, OpenTUI, Bubble Tea, and Ratatui sources were reverified; the declaration and TSX example type-check, and the SFC parses and compiles. No VOUCHED stamp was added. F1.8 now exports the selected surface.

The selected shape keeps `useApp()` as lifecycle operations, replaces `useWindowSize()` with a session-derived `useLayoutSize()`, removes `useIsScreenReaderEnabled()`, and does not expose a mutable renderer or a live `app.session` handle without consumer evidence. `@vue-tui/testing` now exposes the same readonly session snapshot on `RenderResult` because assertions necessarily run outside component setup while components see modeled production facts.

**Completed checkpoint:** F1.4 — implemented and verified the clean-slate live mount surface and authoritative internal session resolver. Optional `mode` replaces the old mode booleans; `liveUpdates` names only output cadence; validation precedes stream reads and terminal mutation; one resolved surface drives output, dimensions, alternate-screen ownership, transcript fallback, hit-map construction, and the private session service. The implementation passed real-PTY, visual-controller, strict Vue 3.4 npm and Yarn PnP tarball, lifecycle, type, and independent-review gates without exporting the still host-incomplete public session.

F1.4 closed the following implementation requirements:

- add optional `mode`, remove `fullscreen` and `alternateScreen`, and validate every accepted input case synchronously before stream reservation or terminal mutation;
- resolve live-host mode, output destination/dynamic updates/presentation, dimensions, and capabilities once, then drive renderer behavior and an internal session service from that result;
- make the internal service available before root setup with stable identity, reactive dimensions, and an immutable capability snapshot, without adding a supported root-package export yet;
- implement the target main-screen screen-reader fallback and every live TTY/non-TTY/default/override row so no public fact describes behavior the runtime did not acquire;
- state the next disposition of current `interactive` and observation semantics wherever they would otherwise create a second truth about output behavior; F1.5 later removed `debug` directly and moved deterministic observation behind an internal observer;
- retain current public fact hooks temporarily, but stop them from creating another live-host resolution source; their direct removal happens when the replacement is actually public;
- include mount-option type/runtime guards, every live-host integration row, lifecycle cleanup, real-PTY evidence, and a clean packed consumer of the new `mode` mount surface.

F1.4 initially left `layout.rows: null` for the then-unbounded Inline implementation. F1.6 now enforces the visual Inline maximum; F1.4 did not redesign deterministic test controls, expose the string-host session, fix ownership itself, or finish suspend/error/final-output lifecycle work.

**Completed checkpoint:** F1.5 — deterministic tests and both string presentations now use the authoritative render-session model. `@vue-tui/testing` models finite production-like mode, presentation, update-cadence, stdin, stdout, and dimension axes through the production resolver; exposes the component-visible session; observes renderer content independently from an xterm-emulated screen; and disposes its app, streams, and emulator through deterministic cleanup. Public `renderToString()` is a fixed visual document host, the internal helper is a fixed screen-reader document host, both use isolated inert streams, recognizable presentation/mode passthrough fails before rendering, and unavailable app operations report failure explicitly. The live and testing `debug` options are removed programming errors, while the internal render observer does not change output cadence or surface resolution. Focused and full repository tests, fresh CI, clean tarball consumers, Inline and Fullscreen visual-controller journeys, terminal restoration, package contents, and independent review all passed.

**Completed checkpoint:** F1.6 — visual Inline now uses terminal rows as a maximum and a hard row-zero paint clip. Framework-generated layout/writer/lifecycle controls and geometry-safe coordinated TTY writes emit no ED2/ED3/Home; committed history and resize-abandoned snapshots are immutable; repeated `app.clear()` forgets its erased baseline; direct raw-stream output remains outside the guarantee; and deliberate destructive main-screen behavior is session-external, raw, or Fullscreen. Runtime, integration, PTY, visual-controller, terminal-restoration, package, clean-consumer, fresh-CI, and independent-review gates passed. Real-PTY evidence selected these mechanisms over Ink's destructive overflow fallback, a global tail slice, or a mounted destructive option.

**Completed checkpoint:** F1.7 — terminal lifecycle is now one exception-safe transaction. Mount failures roll back every acquired resource; normal unmount, app exit, process exit, `beforeExit`, SIGINT/SIGTERM/SIGHUP, suspension, continuation, fatal error, and HMR release only owned raw, paste, mouse, Kitty, cursor, alternate-screen, listener, and stream leases even when one release fails. Ordinary nested lifecycle entry and re-entrant teardown or settlement wait for the current acquisition or repaint to finish; a non-returning `process.exit()` or signal-exit callback instead restores synchronously and immediately without a final user render or Vue lifecycle hook. Supported non-Windows live hosts restore before stopping with `SIGSTOP` and reacquire the same resolved surface after continuation. Clean final-stream output is emitted once, including natural event-loop drain; fatal final-stream output does not replay a stale successful frame. Inline and transcript errors remain as a rich frame only when physically durable and use stderr as a narrow fallback after clipping, lost stdout, or a failed boundary-frame write; Fullscreen restores before writing its durable error to stderr. Focused lifecycle regressions, full `vp run ready`, fresh `CI=true vp run ci`, 158 real-PTY tests, package-content inspection, a clean Vue 3.4/TypeScript 6 runtime-and-testing tarball consumer, image-reviewed Inline and Fullscreen suspend/resume/resize/exit journeys, exact terminal and termios restoration, and independent review passed.

**Completed checkpoint:** F1.8 — published and verified the rendering-session contract. `useRenderSession()` and `useLayoutSize()` plus their named public types are exported from the supported runtime surface; `useWindowSize()` and `useIsScreenReaderEnabled()` are removed without aliases. Public, type, SFC, TSX, HMR, live-host, deterministic-host, and string-host tests cover every session branch, stable readonly identity, nullable `layout.rows`, immutable capabilities, setup misuse, teardown snapshots, reactive resize, and continuation. Resize generations are coalesced so an ordinary live resize never paints an obsolete layout, and continuation repaints the newest live-stream or terminal geometry before raw, Kitty, or other input modes return. Full `vp run ready`, fresh `CI=true vp run ci`, 158 real-PTY tests, exact package/export inspection, a clean Vue 3.4/TypeScript 6 tarball consumer, image-reviewed Inline and Fullscreen resize/interaction/exit journeys, exact termios restoration, and independent reviews passed. No VOUCHED stamp was added.

**Completed checkpoint:** F2 — rendered-target lifetime now has one private per-root contract, recorded in [rendered-target-lifetime.md](./rendered-target-lifetime.md). Registration follows resolved host identity rather than raw Vue-ref identity, reconciles stable component proxies after commits, invalidates removed subtrees synchronously, and performs detach-before-attach with re-entrant cleanup safety. `useDraggable()` and the former `useBoxMetrics()` were the first two adapters; F5 later replaced the latter without changing the private lifetime contract. Null, insertion, removal, retargeting, unmount, nested scope disposal, HMR rerender/reload, Vue 3.4 stale refs, deterministic and string hosts, mouse/raw failure paths, package declarations, a clean consumer, full repository gates, fresh CI, a real PTY, visual inspection, exact terminal restoration, and independent reviews passed. No generic target API or renderer node became public, and no VOUCHED stamp was added.

**Completed checkpoint:** F3 — normalized input and routing now has one serialized physical-stdin ingress, one shared immutable fact, fact-start route identities, an executable global → selected boundary → supplied focus path → delayed default → optional external policy, and transactional input-resource ownership across every supported host and lifecycle. The public `useInput()` contract exposes frozen key, text, paste, or uninterpreted events through one all-run application-global layer and requires a synchronous route result whose four outcomes merge monotonically. `useInputAvailability()` reports stable host capability without acquiring input. Active live registration fails fast on an unavailable host, inactive and string-host registration stay inert, and pointer facts remain private for F6. `usePaste`, public `Key`, public raw-mode operations, mount `rawMode`, and `exitOnCtrlC` are removed directly; the vouched `useStdin().stdin` raw escape hatch remains. Repository consumers, API and type guards, clean packed consumption, HMR, deterministic and string hosts, both rendering modes, real PTYs, visual input journeys, exact terminal restoration, and independent adversarial review agree with the contract. No VOUCHED stamp was added.

F3's four dependency-ordered stages are complete:

1. **Completed:** select normalized UTF-8 from product, implementation and prior-art evidence; prove slow protocol isolation and ordered text, key, paste, uninterpreted and invalid-input delivery through an explicit adapter into a real child PTY in Inline and Fullscreen. Raw source spans are a deliberate non-contract unless a future vouched requirement needs arbitrary original-byte recovery.
2. **Completed:** under the vouched direct-stdin contract, retain `useStdin().stdin` as the actual physical stream selected for the mount. Raw reads have no framework event semantics and no guarantee of safe composition with managed routing; semantic external-owner fallthrough remains a separate route, not a second brokered stream. The implemented host boundary removes the separate public raw-mode surface without changing this escape hatch.
3. **Completed:** selected topologies and public global consumers own transactional input demand across both modes, screen-reader and final-stream output, shared stdin, suspension, continuation, teardown, replacement, independent globals, acquisition rollback, HMR, deterministic final output, controllable and externally pre-raw custom TTYs, non-TTY exclusion, and the real nested-PTY journey. `useStdin()` exposes only the exact mounted stream; public raw-mode operations and mount policy are removed; semantic demand owns raw/paste/listener/ref/Kitty state; input-free final output exits naturally; string hosts stay inert; and error, type, package-consumer, restoration, and adversarial re-entry gates pass.
4. **Completed public contract:** the [public input contract](./input-routing.md#accepted-public-input-contract) replaces `useInput` with one all-run application-global layer over normalized facts, removes `usePaste`, public `Key`, and `exitOnCtrlC`, retains the vouched raw stdin, requires a synchronous four-result handler, publishes an independent semantic-input availability query, fails fast when a registration becomes active on an unavailable live host, keeps inactive and string-host registration inert, and assigns target/local/external attachment to the now-completed F4 contract. Rejected return-control, query-location, unavailable-host, Ctrl+C, numeric-global-priority, and low-level-topology alternatives remain as decision history.

**Completed checkpoint:** F4 — logical focus and focus scopes now build on F2 rendered-target lifetime and F3 normalized routing. The accepted unstamped contract replaces the flat string-ID registry with one app-owned effective target, opaque ref-bound target and scope handles, rendered-preorder traversal, explicit root focus, active and hard-trapped scopes, centralized fallback and restoration, focused-target and scope input, normalized external fallthrough, preventable Tab, and ordinary Escape. The old registry, global enable/disable, setup-order traversal, automatic Escape blur, and per-focus raw path are removed without aliases. Public coding-agent, finder, independent-region, focused key/text/paste, duplicate-host, recoverable invalid-reactive-transition, nested-scope disposal, and no-focus journeys pass across the supported package and both rendering modes. Template, TSX, JavaScript/`any`, HMR, deterministic, string, non-TTY, screen-reader, final-output, suspension, package-consumer, real-PTY, visual-controller, modal-isolation, removal, restoration, exact-terminal-cleanup, full-repository, fresh-CI, and independent-review evidence agree. No VOUCHED stamp was added.

**Completed checkpoint:** F5 — the [audit, public contract, paint-derived geometry authority, geometry cutover, and caret registry](./semantic-geometry-and-caret.md) are implemented. `useElementGeometry()` publishes one frozen accepted paint generation. `useCaret(target, { focus, position })` binds one element-local rendered cell to one exact F4 focus handle, selects at most the effective owner, and publishes explicit frozen state while the existing mode writer remains the private terminal-cursor transport. `useBoxMetrics()`, `measureElement()`, `useCursor()`, and their named legacy types are removed directly.

The completed geometry and caret foundation has focused runtime, integration, relevant PTY, HMR, testing cursor-visibility, and clean Vue 3.4/TypeScript 6 tarball-consumer evidence. Standard and incremental writers retain their last successful output/caret baselines after a failed stream write, so the same frame can retry. The bounded Inline and Fullscreen visual journey proved focus transfer, modal hiding, restoration, buffer ownership, exact terminal cleanup, and shell recovery. Fresh `vp run ready`, `CI=true vp run ci`, and independent review passed, so F5 closed and activated F6.

**Completed checkpoint:** F6 — the [Fullscreen targeted pointer contract](./targeted-pointer.md) is implemented after the source, consumer, API-neutral journey, pinned-peer, naming, and adversarial reviews. `@vue-tui/runtime/fullscreen` exports keyed `useMouseEvent()` for click and wheel plus one left-button `useMouseDrag()` lifecycle owned by the resolved rendered host. Exact F5 fragments, F2 lifetime, fact-start leases, per-host drag cohorts, explicit target/bubble delivery, receiver-local coordinates, demand-selected `1000`/`1002`, and existing F4/ScrollBox composition replace listener props and application-position-owning drag. `@vue-tui/testing` exposes a typed down/move/up/wheel driver at the parsed physical-fact boundary plus modeled button/button-motion state, so it cannot bypass production hit testing, click synthesis, or drag capture. The former listener props, root `useDraggable()`, root `useMouseInput()`, and their types are removed directly. Public/type/package, deterministic, HMR, lifecycle-fault, real-PTY, headless visual, exact-restoration, full local repository, and independent-review gates pass. The direct native macOS Terminal observation proved native selection and scrollback before demand, native drag-selection suppression and target wheel delivery during demand, and restoration after target removal and shell return. F6 is Done and no VOUCHED stamp changed.

**Completed checkpoint:** F7 — the four existing `ScrollBoxExpose` operations now return the selected boolean movement result. `true` means only that the effective top line changed after flooring and clamping; sticky-following changes without movement return `false`. The `void` return is replaced directly without aliases, the private sticky control is not exposed to JavaScript, and invalid numeric inputs fail before mutation. F4 focused-target-to-scope routing and F6 target-to-ancestor wheel routing stop only after inner movement, continue at an edge, and return to the inner owner on reversal. Page Up/Down use the F5 wrapper height. Public/type/runtime, package consumer, deterministic, real-PTY, visual-restoration, full-repository, fresh-CI, and independent-review gates agree. F7 is Done and no VOUCHED stamp changed.

**Completed checkpoint:** F8 — `useTextSelection()` from `/fullscreen` owns complete-grapheme command and pointer selection over exactly one top-level Text, follows successful final-paint provenance, and keeps one active range across the app. Root `useClipboard()` owns one explicit custom or OSC 52 transport with FIFO calls, exact copied/requested/unavailable/rejected results, and exact fallback text. Public/type/runtime/testing surfaces, clean Vue 3.4.38 and TypeScript 6.0.3 packed consumption, deterministic and HMR behavior, long transcript and workbench journeys, real PTY behavior, image-observed shell and suspension journeys, exact terminal restoration, and a native macOS Terminal custom-transport copy agree. Final review corrected stale copy during paint invalidation, exact visual-row retention at duplicated soft-wrap stops, and styled graphemes split across nested Text boundaries. At that historical checkpoint, final `vp run ready` passed all 27 tasks and `CI=true vp run ci` passed all 28 tasks with zero cache hits. F8 became Done at `3d7e197`, its ordinary remote run was green, and no VOUCHED stamp changed. Runtime closure then continued locally from it on the same PR branch; maintainer merge remained outside the development goal.

### Completed foundation sequence

| Checkpoint      | Status | Scope                                                                                                                                                                                                                                                                                                                                                    |
| --------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1.3            | Done   | Select one complete readonly render-session contract and current-API dispositions.                                                                                                                                                                                                                                                                       |
| F1.4            | Done   | Implement the clean-slate live mount resolver and internal session service; do not publish a host-incomplete projection.                                                                                                                                                                                                                                 |
| F1.5            | Done   | Finite production-like test controls, orthogonal renderer-content and xterm-screen observation, modeled session and cleanup, fixed visual/screen-reader string hosts, isolated inert string I/O, explicit unavailable operations, and direct removal of `debug`; repository, CI, package, PTY, visual, restoration, and independent-review gates passed. |
| F1.6            | Done   | Non-destructive bounded Inline ownership, geometry-safe coordinated TTY output, resize recovery, teardown positioning, and all repository, package, PTY, visual, restoration, CI, and independent-review gates are complete.                                                                                                                             |
| F1.7            | Done   | Exception-safe mount/teardown, exact terminal ownership, coordinated suspension/continuation, clean/fatal exit, natural event-loop drain, narrow Inline fatal fallback, deterministic controls, and real-PTY lifecycle evidence are complete.                                                                                                            |
| F1.8            | Done   | Published the readonly render-session hooks and named types, removed the superseded hooks, migrated repository consumers, and passed repository, API/type, PTY, visual, restoration, package, clean-consumer, CI, and independent-review gates.                                                                                                          |
| F2              | Done   | Added one private rendered-host lifetime controller, migrated `useDraggable()` and the then-public `useBoxMetrics()`, rejected detached/stale targets, closed mouse/raw error paths, kept renderer types private, and passed repository, HMR, Vue 3.4 consumer, PTY, visual, restoration, CI, and independent-review gates.                              |
| F3              | Done   | Published normalized immutable input facts, an all-run application-global hook, required route results, stable availability, and route-owned terminal resources; removed the superseded input/raw options and passed repository, host, package, HMR, PTY, visual, restoration, CI, and independent-review gates.                                         |
| F4              | Done   | Published opaque ref-bound focus targets and scopes, centralized rendered-order traversal and restoration, attached normalized input and external fallthrough, removed the old registry/default/raw path, and passed public scenario, boundary, host, package, HMR, both-mode PTY, visual, restoration, CI, and independent-review gates.                |
| F5              | Done   | Published paint-derived semantic geometry and focus-bound caret placement, removed the Yoga measurement and targetless cursor surfaces, and passed public/type, lifecycle, package, HMR, both-mode PTY/visual, restoration, CI, and independent-review gates.                                                                                            |
| F6              | Done   | Published Fullscreen ref-bound click/wheel and drag behavior plus parsed physical mouse testing, removed the old listener and root-hook surfaces, and passed public/type, package, HMR, lifecycle, PTY, headless visual, native GUI, restoration, CI, and independent-review gates.                                                                      |
| F7              | Done   | Replaced all four `ScrollBoxExpose` `void` returns with the accepted boolean top-line-change result and passed public/type/runtime, JavaScript misuse, package, deterministic nested routing, both-mode PTY/visual, restoration, CI, and independent-review gates.                                                                                       |
| F8              | Done   | Published common clipboard transport and Fullscreen one-Text semantic selection, preserved successful-final-paint authority, and passed public/type, package, HMR, long-document, workbench, PTY, visual, restoration, repository, CI-at-checkpoint, and independent-review gates.                                                                       |
| Runtime closure | Closed | Closed R1 through R12 through packed public consumers, exact host/package matrices, J1-J6 capacity and release evidence, final focus semantics, real PTYs, image-observed visuals, and the final local repository gate. R13 through R17 remain Non-blocking.                                                                                             |

## Priority order

| Order | Foundation                      | Status | Current design state                                                                                                                                                                                                                                                                                                                                                                                                                                  | Current implementation state                                                                                                                                                                                                                                                                                                                                                            | Why this order                                                                                                                                                                             |
| ----- | ------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F0    | Fixed full-screen surface       | Done   | The terminal-sized viewport, origin, clipping, coordinated-output, and direct-write contracts are recorded. Runtime closure rejects `/inline` Static on the effective visual surface.                                                                                                                                                                                                                                                                 | Merged in [#254](https://github.com/vuejs-ai/vue-tui/pull/254) as `f1ce02b`; the history boundary is now closed with Runtime.                                                                                                                                                                                                                                                           | A reliable full-screen surface is the prerequisite already completed for addressable geometry and hit testing.                                                                             |
| F1    | Rendering-mode session contract | Done   | Both modes are first-class. F1.2 settled the mount/host policy, F1.3 selected `useRenderSession()` plus `useLayoutSize()`, F1.4 completed their private live behavior source, F1.5 completed deterministic-test and string-host authority, F1.6 completed Inline history and row-bound ownership, F1.7 completed lifecycle behavior, and F1.8 published the verified public surface.                                                                  | Live, deterministic-test, and string hosts share authoritative readonly session facts, and every F1 acceptance gate passed.                                                                                                                                                                                                                                                             | Every capability and mode-specific API now has one truthful answer for what surface is requested, what became effective, and which environment is running.                                 |
| F2    | Rendered-target lifetime        | Done   | A behavior bound to a rendered ref attaches by resolved host identity, follows insertion and replacement, and releases on removal or disposal. Visibility and semantic target types remain later concerns; no generic public target API was selected.                                                                                                                                                                                                 | One private per-root controller drove `useDraggable()` and the former `useBoxMetrics()` across live, deterministic, string, HMR, Vue 3.4 stale-ref, scope, teardown, and failure paths; F5 now reuses it for public geometry, and every F2 acceptance gate passed.                                                                                                                      | Focus and pointer now share one proven answer to `v-if`, retargeting, unmount, and HMR without exposing renderer nodes or prematurely defining their semantics.                            |
| F3    | Normalized input and routing    | Done   | Normalized public facts, a required route result, one all-run application-global hook, an independent availability query, fail-fast live activation, inert inactive and string-host registration, and F4-owned target/local/external attachment are settled. `usePaste`, public `Key`, raw-mode operations and policy, and `exitOnCtrlC` are removed.                                                                                                 | One shared ingress, immutable facts, fact-start routes, independent globals, selected-generation capture, preventable defaults, transactional demand, normalized PTY fallthrough, and every closure gate are implemented. A replaced focus generation fails only the captured selected path closed; independent globals remain eligible.                                                | Routing is required by every interactive scenario and now defines delivery semantics before focus or pointer specializes them.                                                             |
| F4    | Logical focus and focus scopes  | Done   | The accepted unstamped contract selects one app-owned effective target, opaque ref-bound handles, rendered preorder, explicit root focus, hard trapped scopes, centralized fallback/restoration, focused-target/scope/external attachments, and direct replacement of the former focus surfaces. A trapped scope occupies F3's active-boundary layer exactly once.                                                                                    | The old registry, automatic Escape blur, setup-order Tab, and per-focus raw path are removed. Host-only reconciliation reuses a generation only for an exact route plus full rendered/sequential-order no-op with no logical mutation; any semantic change replaces it while preserving F3's stale-selected-path rule. Public, package, PTY, visual, cleanup, and review evidence pass. | Focus selects one owner inside the defined input route instead of inventing its own event model; editors, finders, overlays, and keyboard-driven scrolling can now share this foundation.  |
| F5    | Semantic geometry and caret     | Done   | F5.2 selects direct Vue `ElementTarget`, frozen discriminated geometry with parent/surface bounds plus exact local/parent/surface/visible-surface fragment mappings, `useElementGeometry()`, and `useCaret(target, { focus, position })` with one owner per F4 handle. Private sparse slots cover trailing and empty insertion cells. Marker, logical-offset, generic-handle, and arbitrary-active alternatives are rejected.                         | Geometry, ScrollBox migration, focus-bound caret arbitration, transactional writer transport, testing cursor visibility, HMR, package consumption, and direct removal of the old measurement/cursor APIs are implemented. Public/type, lifecycle, package, HMR, both-mode PTY/visual, restoration, full-repository, fresh-CI, and independent-review gates pass.                        | Editors need a real caret after focus, and pointer needs the same rendered geometry later. Making this explicit avoids hiding coordinates inside the ref-lifetime seam or the pointer API. |
| F6    | Full-screen targeted pointer    | Done   | The selected unstamped contract keeps common visual components passive and adds `useMouseEvent()` for click/wheel plus rendered-host-owned `useMouseDrag()` from `@vue-tui/runtime/fullscreen`. Exact F5 fragments, explicit target/bubble delivery, minimum reporting demand, left-button capture, existing focus/scroll handles, and a parsed-physical-fact testing driver define behavior; former listener props and root mouse hooks are removed. | The guarded runtime/testing surfaces, direct v1 removal, successful-frame route, drag cohort, demand, composition, host, package, HMR, lifecycle-fault, real-PTY, headless visual, restoration, full local repository, native GUI-terminal side-effect, and independent-review gates pass.                                                                                              | F7 uses the proven focus and pointer delivery; F8 now addresses the established native-selection cost of pointer capture.                                                                  |
| F7    | Scroll composition              | Done   | `ScrollBox` remains common, bounded, input-free, and imperative. All four operations return one synchronous boolean that reports only an effective top-line change after flooring and clamping; sticky re-arm without movement returns `false`.                                                                                                                                                                                                       | Public declarations, runtime and JavaScript guards, docs, example, clean packed consumption, component mechanics, F4 keyboard and F6 wheel nesting, both-mode real PTY and visual restoration, repository, CI, and review gates pass.                                                                                                                                                   | Real F4 focus and F6 pointer delivery now compose correctly at both inner edges without duplicating ScrollBox state.                                                                       |
| F8    | Full-screen selection and copy  | Done   | The selected contract keeps clipboard separate from selection: the common root exposes one explicit custom or OSC 52 transport and exact outcomes, while `/fullscreen` binds command and pointer selection to exactly one top-level Text with one active range per app and successful final-paint provenance.                                                                                                                                         | Runtime, testing, public/type, package, HMR, long-transcript, workbench, PTY, image-observed visual, native custom-clipboard, restoration, repository, fresh-CI, and independent-review gates pass on the current branch.                                                                                                                                                               | This compensates for a user-facing cost of targeted mouse capture and depends on focus, pointer, geometry, keyboard commands, and terminal capability reporting.                           |

## Completion criteria by foundation

### F1 — Rendering-mode session contract

F1 is done when:

The mount-model, default, removed-key, and forced non-TTY decision gates below were satisfied by F1.2. They remain listed as concrete requirements because F1 is not Done until implementation, public surface, tests, and records also satisfy them.

- the target mount type exposes optional `mode: "inline" | "fullscreen"`, normalizes omission to an Inline request, and does not treat that default as product hierarchy;
- one canonical `createApp` and the public `mode` term replace separate application creators or mode booleans;
- `fullscreen` and `alternateScreen` are removed directly from the target surface, with exact behavior for recognizable obsolete JavaScript keys so an application cannot silently start under the wrong screen model;
- normal non-TTY output follows the accepted final-stream default, while an explicit live-update override may run a stream updater without claiming a terminal mode or capability;
- application code can distinguish requested/effective mode, render host, output destination/dynamic-update/presentation, terminal/layout dimensions, stable-origin availability, renderer-owned element-hit-testing availability, and suspension support without inspecting private state or `stdout.isTTY`; F3 owns public raw-input availability;
- fallback or failure is explicit for live TTY, non-TTY, static render, deterministic testing, screen-reader mode, and unsupported stream combinations;
- inline live-region ownership says what remains addressable after a terminal-history commit through `/inline` `Static`, coordinated or direct external output, resize, suspension, and restoration; full-screen session behavior exposes the implemented fixed-surface, signal-restoration, direct-write, and Static-rejection boundary without reopening #254;
- suspension and final-output semantics are explicit in both modes;
- `@vue-tui/testing` and `renderToString` expose only the mode controls and observations that are meaningful for their hosts;
- type, API, integration, lifecycle, and real-PTY tests prove the matrix, with every affected component or composable changed only according to its accepted target contract.

F1 does not design pointer events, focus delivery, or a component catalog. It supplies the facts those APIs will consume.

### F2 — Rendered-target lifetime

F2 is done when:

- one internal contract follows the current rendered ref through null, insertion, `v-if` removal, retargeting, component unmount, scope disposal, and HMR;
- registration cannot remain live after its rendered element disappears and cannot accidentally attach twice after retargeting;
- the contract works with Vue template refs and TSX refs without exposing `TuiNode` or another renderer-internal node as public authoring API;
- focused tests reproduce the lifetime class behind #250 and prove cleanup and retargeting;
- two internal registration adapters exercise the same seam without defining focus, geometry, caret, or pointer semantics; publication of a generic shared composable waits for evidence from more than one real behavior.

### F3 — Normalized input and routing

F3 is done when:

- stdin is parsed once into normalized key, text, paste, pointer, or explicitly uninterpreted input without discarding key identity, modifiers, press/repeat/release, or paste boundaries already known by the parser;
- framework-owned terminal protocol replies are removed before application delivery;
- application shortcuts, active regions, component handlers, and an optional external owner have an explicit priority and fallthrough route;
- handled, continue, component-default prevention, and PTY fallthrough are expressible without every component maintaining manual active booleans;
- direct stdin access is retained under the vouched raw escape-hatch contract; the implemented host boundary removes public raw-mode operations and the mount option; the completed migration replaces `useInput` and removes `usePaste` and public `Key` without compatibility aliases;
- a global interrupt, local editor, paste, repeat/release keys, and unhandled-key PTY fallback pass semantic and real-PTY tests in both rendering modes.

F3 defines shared byte ownership and delivery semantics; it does not publish renderer-targeted pointer events. F6 owns that public contract.

Under the accepted [F3/F4 attachment boundary](./input-routing.md#attachment-boundary-publish-one-global-hook-in-f3-complete-local-attachment-in-f4), the active-region, component, ancestor, and external-owner bullets require an executable route plus one reusable public result contract, not a public API for applications to select those recipients manually. Completed F4 publishes the target/local/external attachment while keeping F3's topology private and preserving its order and outcomes. The rejected low-level selection alternative remains in the decision record so the roadmap does not mistake private topology for an ordinary authoring API.

### F4 — Logical focus and focus scopes

F4 is done when:

- one runtime owner maintains the effective logical focus target and restores or advances it predictably when targets become hidden, disabled, removed, or trapped inside a scope;
- focus registration follows F2's rendered lifetime and does not leave an active handler after `v-if` removal or retargeting;
- traversal order, disabled and hidden targets, nested scopes, modal traps, programmatic focus, and restoration after close or unmount are explicit;
- the focused owner receives F3's normalized key, text, and paste events without applications manually connecting `isFocused` to `useInput({ isActive })`;
- current `useFocus` and `useFocusManager` have an explicit target disposition: retain, replace, or remove;
- the coding-agent composer/approval flow, a finder, two independent regions, unmount restoration, and both rendering modes pass semantic and real-PTY tests.

### F5 — Semantic geometry and caret

F5 is done when:

- one semantic element reference can supply the parent-relative and dynamic-render-surface-relative rectangles that supported consumers need without exposing `TuiNode`, Yoga, or paint-buffer internals;
- logical focus, collection active item, text insertion point, selection, focus-bound caret request, physical terminal cursor, and pointer target remain distinct states even when one component coordinates them;
- a focused editor can request caret placement relative to its rendered element in both modes without calculating physical terminal cells itself;
- measurement and caret behavior is explicit when an element is clipped, hidden, removed, retargeted, or rendered under static, screen-reader, test, and non-interactive hosts;
- the pre-F5 `useCursor`, `useBoxMetrics`, and `measureElement` each have an explicit target disposition: retain, replace, or remove;
- template/TSX types, resize, clipping, wide-glyph, ref-lifetime, both-mode, and real-PTY caret tests prove the contract.

### F6 — Full-screen targeted pointer

F6 is done when:

- the F6-selected `@vue-tui/runtime/fullscreen` entry point establishes exactly the two documented mouse value exports and named mouse types, references rather than re-exports root `ElementTarget`/`CellPoint`, and is guarded as public API; completed F8 then deliberately adds `useTextSelection()` and its named types to the same supported subpath, so the current guarded value set contains three exports;
- `@vue-tui/testing` exposes the exact typed down/move/up/wheel physical-fact driver and reporting state selected by F6, with export/type/declaration and clean-consumer guards;
- common `Box`, `Text`, and `ScrollBox` types and runtime behavior reject targeted listener props, including Vue listener fallthrough and JavaScript/`any` cases;
- active effective-visual-Inline use fails immediately and accurately; expected final-output, screen-reader, and string presentations remain inert; effective visual Fullscreen managed-input and local acquisition failures are exact; the xterm-compatible SGR profile and silent-terminal limit are explicit; deterministic test, HMR, and teardown behavior is specified and tested;
- target selection, same-target aggregation, ancestor propagation, deliberate omission of public `target`/`currentTarget` identity, surface/local coordinates, rendered-host drag cohorts, click synthesis, and the narrow continue/consume result are explicit rather than inherited accidentally from shipped v1;
- live consumers acquire only button, drag, or future hover reporting actually required, downgrade when stronger consumers disappear, and disable all levels on every teardown path;
- shipped listener props, root `useDraggable()`, root `useMouseInput()`, and their historical event/target types are removed directly; the vouched actual stdin stream remains a separate unsafe-composition escape hatch and does not acquire mouse reporting;
- click-to-focus uses F4 instead of maintaining a second focus state;
- click, wheel, drag, nested targets, clipping, retargeting, removal, restoration, and selection-side-effect behavior pass type, integration, and real-PTY tests.

Bare hover does not block the first completion of F6 unless a representative journey proves it necessary; it remains an additive extension after the base event contract is stable.

### F7 — Scroll composition

F7 is Done. Real focus and pointer journeys selected the same boolean movement result for every semantic `ScrollBox` operation; nested owners stop after movement and continue at an edge. Type, runtime, JavaScript misuse, nested scroll, package, PTY, visual, restoration, repository, CI, and review evidence agree with the accepted target decision.

### F8 — Full-screen selection and copy

F8 is Done. Its selected [Fullscreen text selection and clipboard contract](./fullscreen-selection-and-copy.md) satisfied every applicable closure gate:

- `useTextSelection()` targets exactly one top-level Text, derives one semantic document rather than accepting duplicate text or a multi-source join policy, and keeps at most one active range across the app;
- complete-grapheme keyboard commands, Shift-click, pointer drag, clear, select-all, copy, compatible-content retention, removal, retargeting, and one-registration-per-Text behavior are explicit;
- only a successfully displayed final-paint generation publishes semantic text, exact stops, surviving highlight cells, and range state; clipping, wrapping, nested styles, overlays, wide and combined graphemes, ambiguous zero-width content, transforms, truncation, and failed output are honest;
- `useClipboard()` remains a separate common app service with one explicit custom or OSC 52 transport, FIFO calls, exact copied/requested/unavailable/rejected results, suspension and disposal behavior, and exact text for manual fallback; OSC 52 never overstates terminal acceptance;
- Inline activation, final and non-terminal output, screen-reader presentation, deterministic testing, string rendering, HMR, suspension, continuation, teardown, and transport failure have explicit behavior without hidden terminal or operating-system clipboard dependencies;
- root and `/fullscreen` exports, named types, template/TSX/JavaScript behavior, testing controls, packed declarations, a clean consumer, docs, and a first-party example agree;
- focused behavior, long-transcript, at least one non-coding-agent journey, real PTY, image-observed visual behavior, exact terminal and shell restoration, `vp run ready`, and independent review passed; its historical remote CI result remains checkpoint evidence rather than a requirement to trigger another run for local record synchronization.

OSC 52 is one transport option, not the selection model or a confirmation that the terminal copied the payload.

## Explicitly parked

- **Targeted pointer in inline mode:** fzf proves it is possible, but vue-tui has no reliable main-screen origin contract. Reopen only when a representative journey justifies origin discovery, invalidation, and terminal compatibility work.
- **Bare hover and all-motion reporting:** add only when a journey needs hover without a pressed button; do not enable terminal mode `1003` preemptively.
- **Renderer optimization and virtualization:** follow the measurement triggers in [performance.md](./performance.md), not this API roadmap.
- **Component catalog expansion:** component prototypes may validate foundations, but publication waits until the relevant foundation is Done and the inclusion bar in [components-design-principles.md](./components-design-principles.md) is met.

## Disposition of the original unresolved list

| Original question or candidate                                                         | Final disposition                                   |
| -------------------------------------------------------------------------------------- | --------------------------------------------------- |
| Clean-slate `mode` replacement for `fullscreen` / `alternateScreen`                    | F1 Done                                             |
| Non-TTY, static-render, screen-reader, test-host, and unavailable behavior             | F1-F8 Done; Runtime host matrix R1-R12 Closed       |
| Target-ref type and rendered lifetime                                                  | F2 Done and reused by the owning public foundations |
| Normalized keyboard, text, paste, priority, and PTY fallthrough                        | F3 Done                                             |
| Logical focus, focus scopes, traversal, traps, and restoration                         | F4 Done                                             |
| Element-relative geometry, measurement, caret placement, and terminal-cursor transport | F5 Done                                             |
| Mouse hook names and signatures; target omission, propagation, and coordinates         | F6 Done                                             |
| Hover and motion without a pressed button                                              | R15 Additive and Non-blocking                       |
| Former `@click` listener props, `useDraggable`, and `useMouseInput` disposition        | Removed directly by F6                              |
| `ScrollBox` movement-result return value                                               | F7 Done                                             |
| Full-screen selection and copy                                                         | F8 Done                                             |
| Runtime R1-R12                                                                         | Closed                                              |
| Runtime R13-R17                                                                        | Original classifications retained; Non-blocking     |

No successor foundation follows automatically. A future foundation requires a representative journey or consumer failure, a reproducible shared lower-layer gap, and an explicit ledger update. Keep detailed final evidence centralized in [Runtime foundation closure](./runtime-foundation-closure.md#final-closure-evidence).
