# Autonomous iteration

> **Status:** unstamped proposed operating contract and live plan. This file does not itself authorize a run, merge, release, or publication. Once a run begins, overwrite the live sections as truth changes; do not turn this into an append-only worklog.

## Objective

Autonomous work should advance the [product goal](./goal.md) through observable user behavior, not maximize commits or internal changes. The loop is successful when it exposes and closes a framework-owned gap in an [active application scenario](./product-scenarios.md#active-application-scenarios) while keeping the terminal, Vue contract, public API, records, and verification gates trustworthy.

Coding agent is the current pilot journey, not the definition of all future work. Inline and full-screen semantics are settled in the product goal, but their product hierarchy is not; an unattended run may collect comparative evidence and may not choose a primary or degraded mode.

## Current live objective

Continue Runtime foundation closure on the existing PR [#265](https://github.com/vuejs-ai/vue-tui/pull/265). F8 is Done at `3d7e197`, the finite closure matrix is committed at `cd9a71b`, and Stage 2 packed-public proof is complete locally at `b762be9`. Stage 3 is active at the Inline terminal-history and `Static` public-contract boundary: select the supported entry, public name, and exact acceptance identity from the remaining honest alternatives before implementing the demonstrated transactional-history repair and complete host matrix.

Operational checkpoint: PR #265 remains the single open foundation PR and the only development vehicle for the remaining closure work. The local branch is four commits ahead of the remote PR head: `cd9a71b` freezes Stage 1, `9dec733` closes managed-input lifecycle gaps, `2d0a407` restores delayed trapped-scope focus, and `b762be9` commits the reproducible packed consumers and issue #266 fixtures. Continue in this worktree and branch, do not create a second PR or wait for an intermediate merge, and do not push merely because Stage 2 completed. Leave the stale PR body untouched until the final technical scope is stable and Yunfei has approved the complete replacement text.

1. **Completed:** F3 publishes normalized facts, the required route result, one all-run application-global hook, stable input availability, and route-owned terminal resources; it removes the superseded paste, key, raw-mode, and Ctrl+C option surfaces while retaining vouched direct stdin.
2. **Completed F4.1 audit:** the pre-F4 focus surface was a setup-scoped flat string-ID registry with stale hidden targets, setup-order traversal, disabled programmatic focus, duplicate-ID ambiguity, no restoration, per-target raw demand, and no F3 focused route. Issue #250, the coding-agent example, ScrollBox, pinned `mo`, `machud`, and Herdr established the journey requirements.
3. **Completed F4.1 journey experiment:** 30 API-neutral tests cover rendered reorder, explicit root focus, hidden/disabled eligibility, temporary restoration and explicit cancellation, prior-order removal fallback, target-specific blur, one-shot autofocus, atomic retarget, independent and reactivated-region memory, nested and sibling traps, empty-parent and atomic-trap-reactivation restoration, stale restoration targets, boundary/ancestor non-duplication, read-only and imperative-operation batch publication, targetless boundaries, finder/editor separation, mode-independent policy, opening/closing fact isolation, complete policy rollback, and cross-policy checkpoint rejection.
4. **Completed F4.1 peer check:** pinned Ink, OpenTUI, Textual, prompt_toolkit, and pi-tui source plus focused suites distinguish object identity, current-tree eligibility, modal boundaries, and centralized restoration from policies that remain vue-tui choices.
5. **Accepted F4.1 contract:** one contract covers ownership, F2 lifetime, target identity, rendered traversal, scopes, hard traps, restoration, F3 focused-target/scope/external routing, the trapped scope's one active-boundary position, exact input demand, unavailable and error behavior, public composable signatures, Escape/Tab defaults, and clean-slate `useFocus`/`useFocusManager` dispositions.
6. **Completed integration prerequisites:** F3 can publish a complete logical selected topology without acquiring physical input, F2 wraps complete reconcile/dispose/invalidation changes in one owner transaction and announces subtree invalidation before cleanup callbacks, and the focus policy can restore the exact last accepted generation after route validation or acquisition failure.
7. **Completed private controller:** one app-owned service derives immutable target/scope/Tab/external F3 generations, exact effective demand, atomic public refs, rendered preorder, inherited hidden state, duplicate-host validation after swaps, reversible rollback, irreversible disposal, fail-closed cleanup, keyed replacement, and inert string behavior. The caller-revision guard prevents a re-entrant candidate from displacing the last accepted physical lease. Twenty-two controller, 30 policy, 19 route-runtime, and 9 rendered-target tests pass inside the 606-test runtime package.
8. **Completed renderer ownership:** live and deterministic apps create the controller after F3 and the render root, pass it to F2 as transaction owner, inject it before Vue setup, and dispose it after F2 but before F3. String rendering uses the same order with inert behavior. A zero-record controller does not select an empty F3 path or interfere with applications that use only globals or an explicit private F3 fixture.
9. **Completed public cutover:** the supported package exposes opaque ref-bound focus targets and scopes, focused and scope input, normalized external fallthrough, and exact manager handles. The flat string-ID registry, per-focus raw acquisition, automatic Escape blur, and setup-order Tab path are removed. Template/TSX/JavaScript, package consumption, HMR, the migrated coding-agent example, host-matrix, suspension, basic both-mode PTY/visual, restoration, full repository, fresh-CI, and independent-review evidence pass.
10. **Completed F4 closure:** public finder, coding-agent, and independent-region journeys pass with identical Inline and Fullscreen traces; focused key/text/paste, duplicate-host rollback, recoverable invalid reactive target/scope options, nested-scope recursive disposal, modal isolation, focused-host removal, restoration, package consumption, real PTY behavior, visual inspection, and exact terminal cleanup agree with the accepted contract.
11. **Completed F5.1 audit:** current and consumer geometry/caret contracts are inventoried; executable probes and durable tests expose global owner cleanup, hidden/clipped/wide-glyph/resize/host failures; representative both-mode and host journeys are defined; pinned peers and real Ink consumers are reverified. The audit selects renderer-derived atomic geometry, per-owner element-local caret declarations, one arbiter, and mode-specific final translation as the internal mechanism without publishing names.
12. **Completed F5.2 public proposal:** direct Vue targets beat a second generic handle; one frozen discriminated snapshot exposes parent/surface bounds and exact local/parent/surface/visible-surface fragment mappings; caret authors supply an element-local rendered cell plus an exact F4 focus handle; one owner binds to one focus handle; private sparse slots represent empty and trailing insertion cells. Marker, logical-offset, arbitrary-active, single-rectangle, and retained targetless-cursor alternatives are rejected. The temporary private policy prototype established representational sufficiency and was removed after equivalent production controller and integration coverage landed.
13. **Completed F5.3 private geometry service:** one per-root service follows F2 attachment and synchronous invalidation, accepts only successful authoritative paint generations, publishes one deep-frozen snapshot, and derives Box plus complete-grapheme Text mappings without splitting Unicode across Vue boundaries. Each frame snapshots live targets and only traces their ancestor paths and observed Text subtrees, preserving ordinary paint cost elsewhere. Exact wrap/hard-wrap, explicit rows, word movement, sparse legal slots, clipping, hidden/zero/Static/Transform precedence, both modes, clear, suspension, resize, retarget, and failed-paint discard pass. Nested synthetic truncation and a target boundary inside one grapheme are explicitly unavailable rather than approximate. Wide-glyph left clipping preserves source columns, all ancestor overflow clips intersect, and Transform output cannot reopen a clipped cell.
14. **Completed F5 public geometry cutover:** implemented and exported `useElementGeometry()`, migrated ScrollBox to the atomic snapshot, and directly removed `useBoxMetrics()` and `measureElement()` from runtime code, declarations, tests, docs, examples, package guards, and clean consumers.
15. **Completed F5 caret registry unit:** one per-app controller joins each request to an exact F4 focus identity and the current accepted paint geometry, selects at most one effective owner, feeds a validated surface cell or no caret into the existing mode writer, publishes frozen `CaretState`, and directly removes targetless `useCursor()` and `CursorPosition`. Frame and writer baselines advance only after successful output, non-TTY suspension/continuation remains unavailable and byte-free for targeted controls, and testing exposes terminal-cursor visibility. Runtime 656/656, the focused integration suite, relevant PTY 33/33, Vite HMR 1/1, testing 72/72, and the clean Vue 3.4.38/TypeScript 6.0.3 tarball consumer pass.
16. **Completed F5 closure:** the bounded Inline and Fullscreen visual-controller journey proves focus transfer, modal hiding, restoration, normal/alternate buffer ownership, exact terminal and termios cleanup, and post-exit shell input. Fresh `vp run ready`, `CI=true vp run ci`, and independent review pass, so F5 closed and activated F6.
17. **Completed F6 audit and public proposal:** repository and pinned-consumer audit found no production dependency on v1 pointer APIs; coding-agent, workbench, and host journeys establish click-to-focus/action, regional wheel, composed divider drag, minimum reporting, lifecycle, and selection-side-effect requirements. Pinned OpenTUI, Textual, prompt_toolkit, Bubble Tea, Ratatui, pi-tui, Ink, and VueUse evidence supports retained hit testing, honest mouse terminology, and a keyed ref-bound Vue shape without supplying the final contract. The selected proposal uses `useMouseEvent()` for click/wheel and one rendered-host-owned `useMouseDrag()` cohort from `/fullscreen`, plus a `@vue-tui/testing` driver that injects parsed down/move/up/wheel physical facts without bypassing production state machines. It omits raw down/up runtime events, hover, mutable DOM methods, public target identity, and a combined availability query, and directly removes the v1 listener/root-hook surfaces.
18. **Completed F6 closure:** the runtime and testing public surfaces, direct v1 removal, successful-paint click/wheel route, target/bubble consumption, focus and passive ScrollBox composition, same-host drag capture, `1000`/`1002` demand transitions, exact failure cleanup, package consumption, HMR, signals, suspension, real PTY workbench, and headless visual/restoration journeys pass. Repeated local `CI=true vp run ci` runs complete all 28 tasks successfully. The direct native macOS Terminal observation then proved the remaining visible side effect across no-target, active-target, target-removed, and restored-shell states: native selection and scrollback were available before demand, native drag-selection was suppressed and wheel input reached the application during demand, and native behavior returned afterward.
19. **Completed F7 evidence audit and historical decision boundary:** one component-mechanics and three nested route target specifications cover relative, absolute, top, bottom, page-sized, partial, edge, and reversed movement through F4 in both modes and F6 in Fullscreen. Running them red proved the former `void` methods could not distinguish a moved inner viewport from its edge. Pinned Textual privately uses actual pointer movement for wheel bubbling, while OpenTUI, Ratatui, prompt_toolkit, Bubble Tea, pi-tui, and Ink do not supply a uniform public result. The behavior required one synchronous transport-neutral top-row-change fact and retained the passive component boundary; boolean, named-literal, and structured signatures remained honest until the maintainer selected Option A in the next unit.
20. **Completed F7 public contract and closure:** Yunfei selected Option A. `scrollToLine()`, `scrollByLines()`, `scrollToTop()`, and `scrollToBottom()` retain their parameters and return `true` only when the effective top line changes after flooring and clamping. Sticky re-arm without movement returns `false`; finite-number validation happens before mutation; an extra JavaScript argument cannot reach private sticky policy. Public types, docs, example, packed Vue 3.4.38/TypeScript 6.0.3 consumption, ordinary component and nested F4/F6 tests, three real-PTY routes, stepwise Inline/Fullscreen visual inspection, terminal and shell restoration, `vp run ready`, zero-cache 28-task CI, and independent review pass.
21. **Completed F8 contract and closure:** `useTextSelection()` from `/fullscreen` owns complete-grapheme command and pointer selection over exactly one top-level Text, publishes only successful final-paint provenance, and keeps one active range across the app. Root `useClipboard()` exposes one explicit custom or OSC 52 mount transport with copied/requested/unavailable/rejected results and exact text for manual fallback. `@vue-tui/testing` models the custom result and records requests without touching the ambient clipboard. Public/type/package, deterministic, HMR, long-transcript, workbench, real-PTY, image-observed visual, native custom-clipboard, restoration, repository, clean-consumer, fresh-CI, and independent-review gates pass. Final review also fixed invalidated stale copy, soft-wrap visual-stop loss, and complete graphemes split by nested ANSI styling.
22. **Completed Runtime closure Stage 1:** `cd9a71b` freezes R1 through R17, J1 through J10, the three pinned consumers, exact issue #266 topologies, the final-artifact requirement for active PR #251, the terminal-history host matrix, and predeclared renderer, output, backpressure, latency, event-loop, and resource bounds. It corrects the old merge-first workflow and keeps all remaining closure work on PR #265.
23. **Completed Runtime closure Stage 2:** `9dec733`, `2d0a407`, and `b762be9` close the generic lifecycle and delayed-focus failures exposed by packed use, commit checksum-pinned consumer migrations and a deterministic coding-agent transport stub, and pass the coding-agent, `mo`, `machud`, and all ignored/piped × input-free/active-input issue #266 journeys against Runtime tarball `7a7c38f64fff43223d5fbadf662504482f8bd2268026c3c103c185acad166456`. Both visual modes, exact terminal restoration, full `vp run ready`, and one justified local `CI=true vp run ci` pass without a push or remote run.

F1 through F8 and Runtime closure Stages 1 and 2 are Done on the current branch. The packed consumers prove that the public foundations compose without ordinary `/internal` or source-tree imports and without application-owned terminal mechanisms. Runtime closure now stops at the exact Stage 3 public-contract boundary: Runtime ownership and the declarative history need are established, but the supported entry, name, and collection identity policy remain a maintainer choice. Completed contracts remain fixed unless a reproducible failure from the frozen closure journeys contradicts them.

## Baseline for the current foundation

- PR [#264](https://github.com/vuejs-ai/vue-tui/pull/264) consolidated the former #255–#264 stack and merged as `25df36d`; normal visual full-screen, F1's public session facts, F2's [rendered-target lifetime](./rendered-target-lifetime.md), F3.1's serialized ingress, F3.2's semantic fact boundary, F3.3's route-lifetime control, and F3.4's private executable policy evidence are now established inputs on `main`.
- Reconcile records, README claims, package descriptions, and open trackers with the latest default branch before implementation; an agent cannot choose a sound target API from stale inputs.
- Preserve the completed [Fullscreen selection and clipboard contract](./fullscreen-selection-and-copy.md). Do not broaden one top-level Text into a global document, merge clipboard into selection state, add an automatic fallback chain, or expose F2's private controller, F3's private topology, F5's insertion slots, or final paint provenance as generic public APIs during integration.

## Pull-request policy

The F3–F8 and Runtime-closure program uses one active PR rather than a stack. Each coherent unit remains a small local Conventional Commit with its own focused evidence, but several commits may accumulate before a push to the same branch and PR. Do not create a new PR for a checkpoint, review batch, corrective follow-up, public-API proposal, or Runtime-closure stage while #265 remains active. The PR's current draft or ready-for-review state does not change the work-selection order; do not toggle that state unless the maintainer asks. A turn, local commit, record synchronization, review boundary, or stage exit is not a reason to push or trigger GitHub Actions.

## Where work comes from

For planned API work, only the one **Active** item in [api-foundation-roadmap.md](./api-foundation-roadmap.md) is selectable. The active item is Runtime foundation closure Stage 3: settle and implement the Inline terminal-history and `Static` public contract, then close the demonstrated transactional acceptance and complete host matrix. The general evidence order below still governs bugs, external contributions, and packaging failures; only a reproducible generic Runtime failure from one of the fixed journeys may add a row.

Choose one independently shippable unit, in this order:

1. a user-visible failure in a deterministic journey from an active product scenario;
2. a reproducible user bug or external contribution that affects framework correctness or an active scenario;
3. repeated difficult behavior in representative applications or real consumers that can become a generic framework capability;
4. a failure in a clean tarball consumer using published APIs and shipped documentation;
5. a regression in an existing objective gate;
6. a bounded comparison with prior art prompted by one of the failures above.

Searching for TODOs, mining another framework for differences, speculative performance work without a benchmark, and building a component only because another project has it are not work sources. The historical open-ended Ink parity sweep found many bugs and then reached diminishing returns; do not restart it.

When several units qualify, prefer the one with clearer user harm, stronger evidence, broader reuse across the active scenarios, and a smaller verifiable change. Coding-agent work has no automatic priority merely because it is the current pilot.

The foundation roadmap is the single ordered backlog for this program. This live plan names only its current Active item; GitHub issues and PRs remain the public tracker and should not be duplicated here.

## Operating loop

1. Fetch the latest remote state and verify the existing PR #265 worktree, branch, remote head, and Git identity without resetting newer work.
2. Read the records map, [goal.md](./goal.md), [product-scenarios.md](./product-scenarios.md), this live plan, and the exact records for the affected area.
3. Recheck open issues and PRs so the unit is not stale, already assigned, or overlapping an external contributor.
4. State the affected application scenario, the user-visible failure, and the objective before/after criterion: a failing test, type error, active-screen predicate, process result, or packaging failure.
5. For a bug, demonstrate the red state before implementing when practical. Use the strongest fitting layer: unit logic, component rendering, real PTY screen, or clean packaged consumer.
6. Implement the smallest coherent change. Update affected examples, public documentation, and unstamped records in the same change.
7. Run focused checks during development and `vp run ready` for a coherent implementation batch. Run a fresh local `CI=true vp run ci` only at a meaningful public-API, core-Runtime, lifecycle, package, environment-sensitive, or final-acceptance boundary.
8. Follow the shipped visual development guide for terminal-visible changes and use the repository controller for this repository. If both modes are affected, inspect both; if one mode is affected, inspect it plus a no-regression journey for the other when practical.
9. Commit independently reviewable units locally. Push only when a substantial externally reviewable phase genuinely benefits from remote evidence or at final acceptance, never more than once in one autonomous run; let at most one ordinary remote run follow that push. Do not merge, release, publish packages, close external work, or send issue or PR comments without the applicable human gate.
10. Overwrite the live objective, candidates, evidence, and blockers as truth changes. A per-run checkpoint is a reporting and synchronization boundary, not the end of Runtime foundation closure.

## Verification rules

- Behavior claims are demonstrated, not inferred from source.
- Public API additions, replacements, moves, and removals include the applicable SFC/template and TSX type checks, public-surface guards, user documentation, repository consumers, and a clean tarball consumer.
- Input, paste, focus, layout, paint, streaming, scroll, cursor, resize, external output, and teardown changes include real-PTY evidence. Raw output bytes alone are insufficient when the claim concerns what is visible on screen.
- The PTY path declares one terminal profile. Start with `xterm-256color`; do not generalize one Unicode-width, keyboard, or color result to every terminal.
- The visual loop feeds terminal query replies back to the PTY and exposes the active viewport, style runs, cursor, named key/text/resize actions, an explicit wait condition, raw transcript, and exit status.
- Silence is not a settled state. Success is an expected screen predicate, a process exit, or another explicit event before a deadline.
- Preserve durable evidence as tests, fixtures, commands, committed artifacts, stable URLs, or commit hashes. Do not cite a temporary screenshot or `/tmp` result as final evidence.
- A gate may be strengthened autonomously. Weakening or deleting a correctness, lifecycle, type-safety, or terminal-restoration check, or changing a test merely to accept new output, requires explicit review.

## Authority after this contract is approved

Autonomous work may:

- fix reproducible renderer, Vue-contract, and terminal-lifecycle bugs without changing intended public behavior;
- address input, paste, focus, streaming, scrolling, cursor, mouse, resize, error, and cleanup gaps exposed by an active scenario journey or real consumer;
- improve `@vue-tui/testing`, examples, starter material, and shipped documentation when the gap is objectively verifiable;
- implement a component or composable whose public shape is already accepted and whose need passes the vouched [inclusion bar](./components-design-principles.md#inclusion-bar--product-driven-and-evidence-backed);
- correct unstamped records and keep them current with the code;
- create local commits and update the single active PR for review.

## Stop and return to the maintainer

Stop before:

- deciding whether inline or full-screen is primary, or making a change that intentionally reduces one mode to a fallback;
- choosing a new public API when multiple honest shapes remain, including input ownership, focus routing, mode-capability APIs, or a generic styled-cell-grid view;
- moving a package boundary, changing the supported Node or terminal range, adding a native runtime requirement, or choosing release or version policy;
- conflicting with a vouched record;
- duplicating or superseding an active external contribution without coordination;
- weakening an objective gate or accepting a result that cannot be reproduced on the untouched base;
- making a broad visual-language decision that active-screen evidence cannot settle;
- continuing after the same underlying approach has failed three times;
- merging, releasing, publishing, or performing another irreversible external action not explicitly granted for the run.

Ordinary implementation choices inside an accepted API and objective do not require a stop. Choose the smallest reversible option, record durable rationale where it will matter again, and keep moving.

## Representative journey for later interaction foundations

When F3–F8 need an end-to-end interaction journey, use a scripted model adapter that emits deterministic events rather than calling a live provider. The reference journey should cover at least:

- entering and editing a prompt;
- bracketed paste or an explicit current limitation;
- token-by-token assistant output;
- a tool call that requires approval;
- an accepted tool result and a rejected tool request;
- long output that exercises native scrollback inline and a bounded viewport full-screen;
- resize from a small declared viewport to a larger one;
- Ctrl+C or normal exit with cursor, raw mode, mouse, and alternate-screen restoration;
- an error state visible to the user.

Run the same state machine with two view adapters:

- **Inline:** completed transcript through the current `Static` mechanism or its accepted replacement, current streaming output and composer in the managed region, native scrollback retained.
- **Full-screen:** persistent layout in the alternate screen, transcript through `ScrollBox` or its successor, explicit internal navigation, and restoration on exit.

The comparison should record where behavior can share one public abstraction and where the terminal model forces a mode-specific capability. It should not hide differences behind a lowest-common-denominator API or choose a product hierarchy.

## Work after the current foundation

The [API foundation roadmap](./api-foundation-roadmap.md#priority-order) is the only ordered continuation. When the Active foundation satisfies its definition of done, mark it Done and activate the next dependency in the same change; do not copy the remaining queue into this file. Independent bugs and external contributions continue to use the evidence rules above without changing foundation order.

## Distillation returned after each run

Return a concise review that contains:

- the affected application scenario and user capability;
- the tests, screen predicates, package trial, or other durable evidence;
- what was attempted and discarded;
- any record or vouch whose scope may be affected;
- inline and full-screen findings without choosing the hierarchy;
- the remaining blocker, current foundation state, and next queued foundation;
- the exact commits and single active PR updated.

No amount of passing autonomous work vouches this plan, the product goal, or any new direction. The maintainer reviews the distillation, decides what to keep, and explicitly vouches only the direction that should continue to hold.
