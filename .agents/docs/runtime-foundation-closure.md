# Runtime foundation closure

> **Status:** historical unstamped closure record for local candidate `e31aa1ae0dd69cb46e31416eb02a77ce83e4d491`. The [Runtime public API decision ledger](./runtime-public-api-decisions.md) supersedes its “foundation complete,” F1–F8 Done, and R1–R12 Closed conclusions. Its packed-consumer, capacity, PTY, visual, restoration, and implementation results remain evidence rather than protection for the former public API. The later implemented candidate is documented in the [Runtime public foundation re-audit](./runtime-public-foundation-reaudit.md). This does not imply that either candidate was pushed, merged, released, or verified by remote CI.

## Outcome and boundary

This record previously classified `@vue-tui/runtime` as the minimum complete library foundation for the recorded conversational, monitoring, and multi-region workbench journeys. The current item-by-item review keeps that conclusion withdrawn while retaining the journeys and checks below as evidence about the existing mechanisms.

This is a bounded conclusion for the recorded scenarios. It does not claim that an unrelated future product can never require another Runtime change. Component-catalog breadth, editing and collection helpers, application data models, releases, compatibility work, 1.0 planning, and speculative optimization do not block closure.

The local candidate was completed in `/Users/yunfeihe/Documents/github-opensource/.worktrees/vue-tui-api-design` on branch `codex/api-foundations-remaining-20260712`. PR #265 remains the historical development vehicle, but its remote head is older than the local candidate. Push, PR-body or ready-state changes, review-state changes, merge, release, and publication are separate maintainer-directed external actions.

## Historical frozen authority snapshot

The following historical external snapshot was taken on 2026-07-14 at 21:28 Singapore time. The repository was fetched before the snapshot. These values are frozen inputs and are not the final local result.

| Fact                             | Frozen value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Worktree HEAD and remote PR head | `3d7e197dda71d02a1b8926982a35b1604fcf17e1`                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Branch                           | `codex/api-foundations-remaining-20260712`                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| PR                               | [#265](https://github.com/vuejs-ai/vue-tui/pull/265), open, non-draft, mergeable                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| PR base                          | `25df36d8751cde4677a50ec7ceb6c7f6eff97411`                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Last ordinary remote run         | [Actions 29332914513](https://github.com/vuejs-ai/vue-tui/actions/runs/29332914513), successful                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Git identity                     | `i.heyunfei@gmail.com`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Local measurement host           | Darwin 25.5.0 arm64, Node 24.18.0, pnpm 11.1.2, Vite+ CLI 0.2.4 with local vite-plus 0.1.22                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Required records                 | `intent.md`, `product-scenarios.md`, `package-layers.md`, `api-contract.md`, `api-design.md`, `api-foundation-roadmap.md`, `autonomous-iteration.md`, `performance.md`, `rendering-mode-matrix.md`, `fullscreen-output.md`, `render-session.md`, `input-routing.md`, `focus-and-scopes.md`, `semantic-geometry-and-caret.md`, `targeted-pointer.md`, `fullscreen-selection-and-copy.md`, `components/scroll-box.md`, `terminal-ui-prior-art.md`, and the relevant Inline/Fullscreen entries in `ink-divergences.md` |

The old merge-first workflow and the previous PR-body draft are void. They are not evidence inputs.

## Public package and ownership map

The package export map after the accepted local Stage 3 cutover is:

| Entry                           | Supported authoring status                  | Values                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `@vue-tui/runtime`              | Supported common root                       | `createApp`, `renderToString`, `Box`, `Text`, `Newline`, `Spacer`, `Transform`, `useAnimation`, `useApp`, `useCaret`, `useClipboard`, `useElementGeometry`, `useExternalInput`, `useFocus`, `useFocusedInput`, `useFocusManager`, `useFocusScope`, `useFocusScopeInput`, `useInput`, `useInputAvailability`, `useLayoutSize`, `useRenderSession`, `useStderr`, `useStdin`, `useStdout`, `kittyFlags`, and `kittyModifiers` |
| `@vue-tui/runtime/inline`       | Supported Inline-history subpath            | `Static`                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `@vue-tui/runtime/fullscreen`   | Supported mode-specific subpath             | `useMouseEvent`, `useMouseDrag`, and `useTextSelection`                                                                                                                                                                                                                                                                                                                                                                    |
| `@vue-tui/runtime/internal`     | Unsupported family-internal integration ABI | Host/Yoga construction, HMR bridge, render observation, deterministic-host adapters, suspension and size probes, screen-reader support, and private test seams                                                                                                                                                                                                                                                             |
| `@vue-tui/runtime/package.json` | Package metadata                            | No authoring API                                                                                                                                                                                                                                                                                                                                                                                                           |

The root currently has 62 named type exports covering mount/rendering, common primitive props, clipboard, input, focus/routing, app/streams, coordinated output, session/layout, geometry/caret, animation, and Kitty input. The Inline subpath has the five named `StaticChildren`, `StaticProps`, `StaticSlot`, `StaticSlotProps`, and `StaticStyle` types; the Fullscreen subpath has 20 named pointer and selection types. Exact value sets are guarded at runtime. Type-only exports remain guarded deliberately and individually, with declaration inspection and clean-consumer proof, because they are erased at runtime and the accepted API policy does not pretend to exhaustively snapshot them.

Runtime has no dependency on another `@vue-tui/*` package. Vue remains a peer. Its runtime dependencies are `@alcalzone/ansi-tokenize`, `ansi-escapes`, `chalk`, `cli-boxes`, `cli-truncate`, `code-excerpt`, `is-in-ci`, `patch-console`, `signal-exit`, `slice-ansi`, `stack-utils`, `string-width`, `wrap-ansi`, and `yoga-layout`.

The accepted direction remains:

```text
@vue-tui/runtime
  <- @vue-tui/use
  <- @vue-tui/components
```

`@vue-tui/components` composes only the public Runtime surface. `@vue-tui/testing` and `@vue-tui/vite` may use `/internal` as deliberate lockstep family adapters, but their public interfaces may not expose Runtime-internal types or make those paths ordinary application APIs. Repository tests may import Runtime source directly; packed applications and ordinary public components or composables may not.

## Historical dated GitHub classification

| Candidate                                                    | Snapshot and evidence                                                                                                                                                                                                                                                                                                                                                                                                                | Classification                                                          | Closure condition                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PR #265                                                      | Open, non-draft, mergeable at `3d7e197`; body still says F3 is active                                                                                                                                                                                                                                                                                                                                                                | Required development vehicle                                            | All work remains on #265; final body is replaced only after technical closure and explicit approval                                                                                                                                                                   |
| [Issue #246](https://github.com/vuejs-ai/vue-tui/issues/246) | Open with no assignee, linked PR, label, or comment. The local repair adds the minimal runtime-dom `style.display` seam only to Box hosts and composes it with authored `display`; no external issue state was changed                                                                                                                                                                                                               | Required generic Vue/Runtime gap; implemented locally                   | Box-rooted SFC compile/type, packed Vue 3.4 runtime, deterministic Inline and Fullscreen, and real Fullscreen PTY journeys pass across layout, paint, focus, geometry, caret, pointer, retained component state, retargeting, show-again recovery, and exact teardown |
| [Issue #266](https://github.com/vuejs-ai/vue-tui/issues/266) | Open. F3 defines non-TTY stdin as `stdin-not-tty`, keeps input-free applications inert, fails an active managed route explicitly, and retains `useStdin().stdin` as the raw escape hatch. Stage 2 reproduced the exact ignored-child-stdin fork and A-pipe-to-B/inherited-child topologies from committed packed fixtures, three times for every input-free and active-input combination                                             | Existing contract; exact packed-host proof complete                     | Both reported fork topologies pass: output-only use does not assume a TTY, while an active semantic route fails with the documented availability result; no automatic nested-PTY forwarding is added                                                                  |
| [PR #251](https://github.com/vuejs-ai/vue-tui/pull/251)      | Open, non-draft, currently conflicting with main; base `3e44c9a`, head `3188b5e90d081817f8ccf304530a903f0bee4e1f`. Disposable integration passes the contribution's tests but proves that its raw U+E000/U+E001 markers corrupt valid user text and mis-pair nested resets. The final local #265 candidate instead carries a structural-span implementation of the contributed behavior with the external source recorded explicitly | External Runtime correctness contribution; safely reimplemented locally | Focused nesting, wrapping, adjacent-style, literal-private-use, Transform, image-observed visual, and packed Vue 3.4 checks pass; final repository evidence is recorded below                                                                                         |

This classification was the frozen input used to bound the work. No other open issue, PR, TODO, competitor feature, or parity difference entered the matrix without a reproducible generic Runtime failure from a fixed journey.

## Historical pinned packed-consumer inputs

All three consumers start from immutable commits. Disposable verification may edit clones, but final evidence must be reproducible from committed patch files and a script under `packages/runtime-tests/consumers/runtime-foundation/`.

Stage 2 exposes one exact entry command per consumer:

```sh
node packages/runtime-tests/consumers/runtime-foundation/verify.mjs --consumer coding-agent
node packages/runtime-tests/consumers/runtime-foundation/verify.mjs --consumer mo
node packages/runtime-tests/consumers/runtime-foundation/verify.mjs --consumer machud
node packages/runtime-tests/consumers/runtime-foundation/verify.mjs --consumer fork-stdin
```

For each external pin the verifier creates a fresh disposable clone, checks out the exact commit, runs `git apply --check` and `git apply` on the matching committed patch, rewrites only the declared vue-tui dependency to the freshly packed absolute tarball path, runs `vp install --no-frozen-lockfile`, and then runs the commands recorded below. It fails if product source imports `/internal`, a vue-tui source path, or an unlisted registry vue-tui package for Runtime-owned behavior. The verifier records its clone, tarball checksum, patch checksum, commands, exit status, and terminal-restoration assertions; no disposable checkout is final evidence by itself.

The packed artifacts produced from the frozen head during the inventory were historical inputs:

| Package            | Version | SHA-256                                                            |
| ------------------ | ------- | ------------------------------------------------------------------ |
| `@vue-tui/runtime` | 0.1.1   | `c89935219526598e90c023649f9117662c7d7439dfa9f8a4dc61831cd6b95c89` |
| `@vue-tui/vite`    | 0.2.0   | `81b5a277c395a4cf179e9c831162c33894e8bbfb237f035235eef6e635d79234` |

The reproducible package-production pattern is:

```sh
vp run @vue-tui/runtime#build
pnpm --dir packages/runtime pack --pack-destination "$TARBALL_DIR" --json
vp run @vue-tui/vite#build
pnpm --dir packages/vite pack --pack-destination "$TARBALL_DIR" --json
```

### First-party coding agent

- Source: `vuejs-ai/vue-tui@3e44c9a266e52ebeba2db669b4bb96521b9e2f3a`.
- Path: `examples/coding-agent`.
- Package manager: pnpm 11.1.2; Vite+ 0.1.22 line.
- Migration source: the exact existing PR change from the pin to the current `examples/coding-agent/src/app.vue`, plus tarball dependency paths. It replaces legacy global input with normalized events and public focus targets/scopes.
- Build:

```sh
vp install --no-frozen-lockfile
vp run @vue-tui/example-coding-agent#build
```

- Launch profile: `DEEPSEEK_API_KEY=stub CI=false FORCE_COLOR=3 node --import="$VUE_TUI_ROOT/packages/runtime-tests/consumers/runtime-foundation/coding-agent-fetch-stub.mjs" examples/coding-agent/dist/main.mjs` under an `xterm-256color` real PTY. The preloaded fetch stub returns the committed finite OpenAI-compatible SSE sequence and performs no network request.
- Script: type `alp你🙂`, verify exact visible input, drive deterministic streaming and approval through the committed local agent stub, accept and reject one tool action, then send Ctrl+C.
- Assertions: ordered once-only completed history, live streaming tail, composer-to-approval focus isolation and restoration, Unicode input, no `/internal` or source import, exit, cursor/raw/paste state restoration, and working shell input.

The committed Stage 2 verifier now passes the complete packed journey. The targetless trapped approval boundary handles Enter and Escape even before its `v-if` target attaches; printable input cannot leak to the composer; accepted and rejected tool calls, streaming, Unicode input, ordered once-only history, Ctrl+C, terminal restoration, and post-exit shell input all pass. The remaining terminal-history contract is Stage 3 work rather than a missing consumer migration.

### mo finder

- Source: `liangmiQwQ/mo@6bea467a6995f4912e809b417b5c56a3964cc556`.
- Paths: `src/components/selector.vue` and `src/utils/selector.ts`.
- Package manager: pnpm 11.5.0; Vite+ `^0.2.4`.
- Minimal migration: normalized input events with explicit `"consume"`/`"continue"` results; arrow names `"up"`/`"down"`; remove `exitOnCtrlC`; replace both 50 ms delays with `await app.waitUntilRenderFlush()`; point the Runtime dependency at the packed tarball.
- Build and checks:

```sh
vp install --no-frozen-lockfile
vp run build
vp check
vp run test
```

- PTY script: launch the installed disposable package, type `vite`, observe the filtered result, press Enter, observe the success frame, and verify terminal restoration.
- Exact installed launch: `HOME="$CONSUMER_HOME" PATH="$CONSUMER_PREFIX/bin:$PATH" "$CONSUMER_PREFIX/bin/mo" cd`, with the committed fixture config pointing at the committed three-repository directory shape.
- Assertions: only the public Runtime root is imported, no timeout is needed for flush correctness, filtered navigation and acceptance are correct, Ctrl+C cancellation remains application-owned, and the shell is restored.

The committed Stage 2 verifier passes build, tests, installed-package accept and cancel journeys, exact flush-before-unmount behavior, and terminal restoration. The pin's 39 pre-existing `node(no-sync)` warnings remain recorded baseline noise rather than a Runtime failure.

### machud monitor

- Source: `hyfdev/machud@a51a6853686eb818471d0027d2549e6e664c9b36`.
- Paths: `src/App.vue`, `src/main.ts`, `src/components/Graph.vue`, and `src/components/Sparkline.vue`.
- Package manager: pnpm 11.8.0; Vite+ 0.1.24.
- Minimal migration: `useWindowSize()` to `useLayoutSize()`; `useBoxMetrics()` to `useElementGeometry()` and resolved `parent.width`; normalized input with explicit results; remove try/catch capability inference; `alternateScreen: true` to `mode: "fullscreen"`; point the production Runtime dependency at the tarball.
- Build and checks:

```sh
vp install --no-frozen-lockfile
vp run build
vp run test
vp run verify
COLUMNS=120 LINES=40 FORCE_COLOR=3 node dist/machud.mjs --once
CI=false FORCE_COLOR=3 node dist/machud.mjs
```

- PTY script: launch at 120x40, verify fixed Fullscreen layout and responsive graph geometry, drive one live theme change, press `q`, and inspect restoration.
- Assertions: 30 component tests and 108 canonical verification assertions pass; only public root Runtime imports appear in product code; alternate screen, cursor, paste, termios, and shell input restore exactly.

The committed Stage 2 verifier passes all 30 component tests, the 108-assertion canonical verification floor, the 120-column snapshot, a live 120x40 to 100x40 geometry transition, a visible theme change, quit, alternate-screen restoration, terminal state restoration, and post-exit shell input. The pin's 31 formatter failures are pre-existing and `vp check` is not a usable pin-level gate. Old registry `@vue-tui/cli` and `@vue-tui/testing` packages are dev-only and do not supply runtime-owned product behavior; final proof packs Runtime and any repository integration package actually used by the journey.

## Fixed journeys and workloads

Every functional journey was specified to run once in the deterministic host, once in the declared real-PTY profile when terminal behavior is claimed, and once through the packed consumer where listed. Timing workloads used three untimed warmups followed by ten measured repetitions on the local host. The final acceptance in this record is local; no new GitHub Actions run was requested for closure.

The real-PTY profile is Node 24 on Darwin arm64 with `TERM=xterm-256color`, `FORCE_COLOR=3`, UTF-8, and the dimensions listed below. A final image-observed visual-controller pass is required after terminal-visible implementation changes.

| ID   | Journey                           | Fixed workload                                                                                                                                                                                                                                                                                                                                              | Host and repetitions                                                                                                                                                                                  | Required visible and semantic result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| J1   | Inline conversational application | 100x30; 500 completed records of two semantic lines each; 240 token updates to one live response; one accepted and one rejected approval; resize 100x30 to 72x20 and back; one suspension/continuation; one coordinated stdout and stderr record                                                                                                            | Deterministic once; real PTY once; packed first-party once; 3 warmups + 10 local timing runs                                                                                                          | Every completed ID appears in terminal-owned history exactly once and in order; pre-app history is untouched; only the current response, approval, and composer remain live; focus and input restore; resize abandons rather than rewrites old history; coordinated records remain ordered; final shell and terminal state restore                                                                                                                                                                                                                                                                                       |
| J2   | Inline finder                     | 100x30; 2,000 candidate records in application data with at most 28 rendered result rows; query sequence `v`, `vi`, `vit`, `vite`, Backspace, `e`; 200 alternating up/down actions; accept once and cancel once                                                                                                                                             | Deterministic once; real PTY once; packed mo twice, one accept and one cancel; 3 + 10 timing runs                                                                                                     | Filtering, active row, viewport, acceptance, cancellation, flush, and exit are exact; no Runtime internal import or terminal mechanism is reimplemented; native history outside the bounded finder is untouched                                                                                                                                                                                                                                                                                                                                                                                                          |
| J3   | Fullscreen long document          | 100x30; one 500-line top-level Text document with 72 visible cells per line and mixed ASCII, CJK, combining, and ZWJ graphemes; 200 line-scroll operations; 100 selection moves including vertical preferred-column movement; one pointer drag and one copy                                                                                                 | Deterministic once; real PTY once; 3 + 10 timing runs                                                                                                                                                 | Fixed origin and clipping remain correct; selected text and inverse cells match complete graphemes; failed or covered cells are not highlighted; copy text is exact; alternate screen, cursor, mouse, paste, termios, and shell restore                                                                                                                                                                                                                                                                                                                                                                                  |
| J4   | Sparse monitor                    | 120x40; 120 mounted metric rows across six columns; 300 updates, each changing one value and one sparkline point; input `q` after the final update                                                                                                                                                                                                          | Deterministic once; packed machud once; real PTY once; 3 + 10 timing runs                                                                                                                             | Every observed screen is internally coherent; the final values equal the input sequence; no mixed geometry generation appears; input remains responsive; fixed Fullscreen restoration is exact                                                                                                                                                                                                                                                                                                                                                                                                                           |
| J5   | Multi-pane workbench              | 120x40; four panes with 100 mounted rows each; two independently remembered focus scopes; 200 sparse updates distributed round-robin; 100 focus switches; 40 regional wheel actions; 20 divider drag moves; open and close one trapped overlay                                                                                                              | Deterministic once; real PTY once; 3 + 10 timing runs                                                                                                                                                 | Input, focus, pointer, scroll, geometry, clipping, and overlay isolation stay inside the selected region; no PTY/VT engine, persistence, external cell surface, or terminal-pane forwarding is manufactured; the final frame and terminal restoration are exact                                                                                                                                                                                                                                                                                                                                                          |
| J6-I | Slow Inline output                | J1 live tail reduced to 100 committed history items plus 200 replaceable live-frame updates, then repeated with 1,000 history items and 2,000 updates; one 1 KiB coordinated record every tenth update; `maxFps: 0`; Writable `highWaterMark=256` bytes; the first backpressured `_write` callback is held for 200 ms and later callbacks are delayed 20 ms | Deterministic slow-stream harness, 5 repetitions at each volume                                                                                                                                       | The producer mutates once per 1 ms timer turn, awaits only Vue `nextTick()`, never awaits render flush or stream drain, and asserts that `write()` returns `false` before continuing. Committed history and accepted coordinated records remain ordered and exact; future history remains application-owned until it can commit; obsolete replaceable frames do not accumulate; final visible and final output is the newest state; flush and teardown settle after drain                                                                                                                                                |
| J6-F | Slow Fullscreen output            | 120x40 J4 surface with 200 replaceable sparse updates, then 2,000; one 1 KiB coordinated record every tenth update; `maxFps: 0`; same 256-byte writer with the first backpressured callback held for 200 ms and later callbacks delayed 20 ms                                                                                                               | Deterministic slow-stream harness, 5 repetitions at each volume                                                                                                                                       | The same 1 ms/`nextTick()` producer runs without awaiting render flush or drain and must observe backpressure. Alternate-screen/setup order is preserved; only accepted frame generations drive geometry, pointer, caret, and selection; obsolete blocked frames coalesce to the latest; final viewport and restoration bytes are ordered                                                                                                                                                                                                                                                                                |
| J7   | Vue `v-show`                      | One stateful Box subtree with focus, caret, geometry, and Fullscreen pointer attachments; hide, update while hidden, show, retarget, and unmount                                                                                                                                                                                                            | SFC compile/type test; deterministic Inline and Fullscreen; real PTY for terminal-visible state                                                                                                       | Hidden content neither lays out nor paints and cannot own focus, caret, geometry visibility, or hit testing; component state and logical handle survive; show-again publishes one fresh generation without stale input or terminal ownership                                                                                                                                                                                                                                                                                                                                                                             |
| J8-A | Ignored child stdin               | Program A forks a packed vue-tui child with exactly `stdio: ["ignore", "inherit", "inherit", "ipc"]`; run an input-free app and then an app that activates `useInput`                                                                                                                                                                                       | Packed subprocess fixture, three repetitions for each app                                                                                                                                             | The input-free child renders final output and exits without assuming stdin exists; the active route fails with the exact unavailable-input contract before terminal mutation; IPC and inherited output remain usable                                                                                                                                                                                                                                                                                                                                                                                                     |
| J8-B | Inherited piped stdin             | Program A starts B with `stdio: ["pipe", "inherit", "inherit", "ipc"]`; B forks the packed vue-tui child with `stdio: ["inherit", "inherit", "inherit", "ipc"]`; run the same input-free and active-input pair                                                                                                                                              | Packed three-process fixture, three repetitions for each app                                                                                                                                          | The inherited pipe is not misreported as a TTY: input-free final output works, while active semantic routing fails explicitly with `stdin-not-tty`; no automatic byte forwarding, fake TTY capability, or nested PTY policy is added                                                                                                                                                                                                                                                                                                                                                                                     |
| J9   | Nested color reset                | Parent colored Text with nested `color="revert"` and `color="initial"`, including wrapping and an adjacent styled sibling                                                                                                                                                                                                                                   | Disposable #251 integration as evidence, then focused deterministic and packed checks from final #265                                                                                                 | Nested content returns to terminal default foreground without breaking later style restoration in the final packed #265 Runtime; ownership and landing of the external contribution are explicit                                                                                                                                                                                                                                                                                                                                                                                                                         |
| J10  | Terminal-history host matrix      | Two independent history regions each commit A, append B, keep one dynamic tail, then exercise resize, suspension/continuation, coordinated output, normal teardown, pre-handoff failure, accepted-write-plus-later-frame failure, and committed-prefix replacement                                                                                          | Visual Inline, visual Fullscreen, screen-reader fallback, default and explicit-live non-TTY streams, deterministic modeled hosts, and string rendering; each case once plus three failure repetitions | Every host has one accepted supported, unsupported, or fail-fast result. Supported stream hosts expose exact bytes and once-only visibility or serialization. Effective visual Fullscreen rejects presence before history bytes, dynamic output, observation, or a new frame: when setup has acquired nothing it owns nothing, while prior setup-owned leases are restored before the fatal report. Pre-handoff failure remains uncommitted, a normally returned write commits even when it returns `false`, and no accepted item is retried. Multiple regions and the Vue scoped-slot `{ item, index }` contract remain |

## Acceptance bounds and final retention protocol

The correctness, ordering, responsiveness, and backpressure bounds below were chosen before running the committed harnesses and were not moved after observing results. The original record also proposed an aggregate post-GC heap-versus-control heuristic. Preliminary schema-3 and schema-4 executions showed that this heuristic was not an ownership-specific release oracle: repeated runs of the same implementation could report either a positive excess or a large negative delta, and the isolation and feedback-warmup corrections in `cde552a` and `1de3960` did not make a positive aggregate delta identify a surviving Runtime object.

Before the authoritative formal measurements, commit `9a5e16f` replaced that heuristic with schema 6's direct tracked-lifetime protocol and committed its positive controls. Schema 6 explicitly supersedes schemas 4 and 5. This changed the release oracle, not any correctness, latency, heartbeat, ordering, or backpressure limit: release acceptance now requires calibrated reachability census, complete target coverage, zero surviving tracked Runtime witnesses, exact Runtime resource release, and Yoga return to its pre-mount baseline. The current-context aggregate memory estimate remains a recorded diagnostic and neither passes nor fails acceptance by itself. Earlier heap-rule artifacts are preliminary historical evidence, not final results.

### Correctness and ordering

- Every deterministic assertion and packed-consumer assertion must pass exactly; snapshots cannot be weakened to accept current output.
- Inline emits no framework-generated ED2, ED3, or Home, never rewrites pre-app history, and preserves once-only history order across resize, coordinated output, suspension, continuation, normal teardown, and a documented fatal path.
- Fullscreen keeps one fixed successful-frame authority for visible cells, geometry, caret, pointer, and selection and restores every acquired mode on every exit path.
- A history item becomes committed when its complete output transaction's `write()` call returns normally. A `false` return is accepted backpressure and counts as committed. A throw is an indeterminate fatal handoff and is never retried automatically. Paint, observation, or pre-handoff failure must not advance the history cursor.
- The accepted collection signature defines positional acceptance identity explicitly: after an item is committed, every position in that prefix must remain `Object.is`-identical. Replacement arrays and changes in the uncommitted tail are allowed; shrinking, replacing, or reordering a committed position fails clearly; mutation through the same object identity does not rewrite emitted history; remounting begins a new region.

### Responsiveness

- These budgets are user-visible failure guards for the fixed product-sized journeys, not a requirement to make small work scale only with its affected area. The 500-record transcript, 2,000-record finder model, 500-line document, 120-row monitor, and four-pane workbench deliberately exceed the ordinary initial examples while remaining bounded. Passing them accepts the current full-render architecture even when internal work still scales with the mounted surface.
- Input-to-visible latency has p95 no greater than 200 ms and no action greater than 500 ms for J2 through J5. The 200 ms outer line is the direct-feedback requirement for filtering, navigation, approval, scrolling, selection, and pane switching in the recorded interactive scenarios; the 500 ms line prevents a visibly stalled action from disappearing inside a percentile. If p95 is greater than 100 ms but no greater than 200 ms, the reasonable immediate-feedback alternatives could change the conclusion, so return to Yunfei with the measurements and the cost of meeting 100 ms before accepting the architecture.
- A 20 ms event-loop heartbeat has p99 excess delay no greater than 100 ms and no excess delay greater than 200 ms during each workload, so framework work cannot consume the complete direct-feedback budget before an input callback can run.
- The latency sample population is every scripted action-to-observed-screen transition across the ten measured repetitions: query edits and navigation in J2, scroll/selection/copy in J3, metric mutations and quit in J4, and focus/wheel/drag/overlay actions in J5. Warmups are excluded; p95 and p99 use the nearest-rank estimator over the pooled samples for one journey. Render/paint duration is recorded separately for diagnosis but has no independent architecture gate.
- Wall-clock values are local acceptance evidence and are recorded with hardware, Node, repetition count, raw samples, and estimator. CI checks the harness structure and deterministic work counts but no timing threshold.

### Resource and backpressure

- Runtime-owned pending output is bounded independently of producer volume: at most one transaction handed to the stream, no further stream write after `write()` returns `false` until `drain`, and at most one latest replaceable desired frame per application. New declarative history remains in application-owned Vue state until it can be prepared and committed; Runtime may not copy each blocked generation into a byte queue.
- A coordinated-output call made while the stream cannot accept another transaction returns `CoordinatedWriteResult`: accepted writable output, accepted backpressure with `ready`, or blocked non-acceptance with `ready`. A blocked call does not run its body or retain caller bytes in an unbounded Runtime queue.
- On a normal writable, output order is FIFO and `waitUntilRenderFlush()` does not resolve until all accepted bytes for the requested generation have reached the stream callback barrier.
- After the first `write()` that returns `false`, Runtime may not call `write()` again until `drain`. The stream's `writableLength` may exceed its 256-byte high-water mark only by the largest single atomic output transaction already handed to it.
- After the finite producer stops, queued non-coalescible records drain in order, the latest dynamic frame becomes visible, all queue and listener state returns to zero, and teardown restores the terminal.
- Every measured workload and matched control must return Runtime transaction, frame, listener, timer, focus, geometry, pointer, selection, terminal-mode, and other tracked resource counts exactly to zero, and every Yoga node created during that execution must be freed so the live count returns to its pre-mount baseline.
- The schema-6 probe must pass its released and deliberately retained positive-control calibration for both the workload and matched control. In every warmup and measured repetition it must observe all ten declared target kinds, report a consistent witness census, and find zero surviving target kinds and zero surviving witnesses after teardown.
- Current-context reachable-JavaScript memory estimates and ordinary process memory values are diagnostic only. J1 through J5 record ten measured samples and the matched-control delta; J6 has only five repetitions and therefore does not claim a memory trend. A positive diagnostic delta does not override the direct lifetime, Yoga, or Runtime resource evidence.

If a stable result falls within 20% of a latency limit, repeat it on a second local run. If the result still lies in that band and a reasonable alternate bound would change whether the current architecture is accepted, stop and return to Yunfei with both bounds, consequences, measurements, and a recommendation. Do not move a bound after seeing the result.

## Finite closure matrix

| ID  | Requirement or candidate                                                                                                   | Journey                       | Classification                                          | Final status | Observable exit                                                                                                                                                                                        |
| --- | -------------------------------------------------------------------------------------------------------------------------- | ----------------------------- | ------------------------------------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| R1  | F1-F8 behavior and ownership                                                                                               | J1-J10 as applicable          | Required accepted baseline                              | Closed       | No frozen journey contradicts a completed contract; all applicable focused gates remain green                                                                                                          |
| R2  | Packed public Runtime sufficiency                                                                                          | J1, J2, J4                    | Required                                                | Closed       | First-party, mo, and machud build and run from committed patches and packed packages with no ordinary `/internal` or source imports                                                                    |
| R3  | Terminal-history disposition, signature, package placement, and public name                                                | J1, J6-I, J10                 | Accepted clean-slate cutover                            | Closed       | `Static`, its existing prop/slot shape, the `/inline` entry, exact value/type exports, host responses, VOUCHED constraints, and migration agree                                                        |
| R4  | Transactional history acceptance                                                                                           | J1, J6-I, J10                 | Required generic Runtime repair                         | Closed       | Prepare/accept/abandon behavior proves no pre-handoff loss, no accepted-write duplicate, no loss of a re-entrant append, `write(false)` acceptance, and honest fatal ambiguity                         |
| R5  | Collection identity when the accepted history API remains collection-shaped                                                | J1, J10                       | Accepted committed-prefix identity                      | Closed       | Every committed prefix position remains `Object.is`-identical; invalid shrink, replacement, or reorder fails clearly; replacement arrays, uncommitted-tail changes, and remounting have exact behavior |
| R6  | Output coalescing and backpressure                                                                                         | J6-I, J6-F                    | Accepted coordinated-output contract                    | Closed       | All slow-writer correctness and bounded-state rules pass                                                                                                                                               |
| R7  | Renderer capacity                                                                                                          | J1-J5                         | Required measurement                                    | Closed       | Every fixed workload meets correctness, latency, event-loop, and resource bounds                                                                                                                       |
| R8  | Vue `v-show`                                                                                                               | J7                            | Required generic Runtime repair                         | Closed       | Box-rooted SFC semantics pass without translating `v-show` into an application workaround; unsupported Text, Transform, and Static roots are not presented as partial support                          |
| R9  | Fork/non-TTY stdin #266                                                                                                    | J8-A, J8-B                    | Required packed proof of existing contract              | Closed       | Both exact topologies and their input-free/active-input pairs pass and add no fake TTY or hidden forwarding policy                                                                                     |
| R10 | Color reset PR #251                                                                                                        | J9                            | External behavior safely reimplemented locally          | Closed       | The external behavior passes focused and packed checks from the local candidate; adversarial cases explain why the implementation does not copy the unsafe marker mechanism                            |
| R11 | Exact public value guards and deliberate named-type coverage                                                               | J1-J4 package gates           | Required package closure                                | Closed       | Root, `/fullscreen`, `/inline`, testing, and declarations have exact runtime-value guards, individual named-type checks, declaration inspection, and clean-consumer proof                              |
| R12 | `@vue-tui/testing` and `@vue-tui/vite` internal adapters                                                                   | Packed repository gates       | Deliberate family-internal integration                  | Closed       | Their public types expose no Runtime internal type or unsupported authoring path                                                                                                                       |
| R13 | Editor, finder, viewer, collection, overlay, commands, and keymaps                                                         | J1-J5 application composition | Higher layer                                            | Non-blocking | The journeys compose through public Runtime; repeated state behavior may later enter `use` or `components` without changing Runtime ownership                                                          |
| R14 | PTY/VT engine, persistence, detach, transport, process detection, terminal-pane forwarding, and public styled-cell surface | J5 boundary                   | Application-specific or unsupported by current evidence | Non-blocking | No fixed journey requires Runtime to own them; a future real consumer is the evidence trigger                                                                                                          |
| R15 | Hover/all-motion, Inline targeted pointer, multi-click, multi-Text selection                                               | Current recorded scenarios    | Additive                                                | Non-blocking | Parked until a concrete journey requires the exact capability                                                                                                                                          |
| R16 | CJS, starter breadth, component catalog, release, compatibility, 1.0, and npm publication                                  | None                          | Compatibility, tooling, component, or release work      | Non-blocking | Explicitly outside Runtime foundation closure                                                                                                                                                          |
| R17 | Packed buffers, damage tracking, virtualization, native core, or Yoga replacement                                          | J1-J6 measurements            | Unsupported unless a fixed workload fails               | Non-blocking | The accepted full-render architecture passed the frozen bounds; reopen only on recorded representative failure and follow `performance.md`                                                             |

## Completed stage order and continuing stop boundaries

1. The finite record and corrected live records were committed before the new Runtime implementation.
2. Consumer patches and a deterministic first-party agent stub were committed, then all packed consumers and J8-A/J8-B were rerun.
3. The R4 transaction repair, `/inline` `Static` contract, Box-rooted R8 repair, and complete host/package matrix were finished.
4. The workload harnesses and the schema-6 release protocol were committed before authoritative formal J1-J6 measurement. Every unchanged correctness, responsiveness, ordering, and backpressure bound passed, and the final tracked-lifetime, Yoga, and Runtime resource release gates passed, so the current rendering architecture was accepted for the frozen workloads.
5. Technical closure completed through local focused, package, deterministic, PTY, visual, restoration, repository-ready, capacity, and independent-review gates. Push, remote CI, PR-body or ready-state changes, merge, release, and publication remain separate maintainer-directed actions outside this goal.

Stop before changing Runtime ownership, package direction, the Inline/Fullscreen hierarchy, supported runtimes or terminals, Yoga or the core scheduler/output model, adding a native dependency, publishing a coarse terminal-cell surface, duplicating PR #251, or choosing among multiple honest public signatures without Yunfei.

## Historical Stage 1 exit

Stage 1 exited when this record, the records map, roadmap, and autonomous plan agreed and were committed. The work list remained finite at R1 through R17.

## Historical Stage 2 exit

Stage 2 is complete locally in `9dec733`, `2d0a407`, and `b762be9`. The committed verifier applies checksum-pinned patches to the first-party coding agent at `3e44c9a`, `mo` at `6bea467a`, and `machud` at `a51a6853`, installs freshly packed public packages, rejects ordinary `/internal` and source-tree product imports, and passes each declared build and real-PTY journey. The final Runtime tarball used by all four verifiers has SHA-256 `7a7c38f64fff43223d5fbadf662504482f8bd2268026c3c103c185acad166456`; the coding-agent, `mo`, and `machud` patch checksums are respectively `472a0704d3fef2c2d0847452b0baab7fb501a4473d7a312074029b0557108ba6`, `fa624015de1de3c563d9c95f55332f1c618b2ad238692e9e687bb2b997628751`, and `196c404c697712c5c8898a8426cc1a977c7a974d6a881141461c476277b336ce`.

J8-A and J8-B pass all ignored and piped stdin combinations for input-free and active-input children, with three repetitions per combination. Input-free children emit their final document exactly once and exit; active semantic input reports `stdin-not-tty` before raw, listener, cursor, alternate-screen, paste, Kitty, mouse, synchronized-output, or child-output mutation. No fake TTY, byte forwarder, or nested-PTY policy was added, so R9 is closed by packed evidence.

The fixed consumer journeys exposed two generic implementation gaps and one consumer routing error. Runtime now registers `useInput` cleanup before immediate activation can throw, treats the preflight-known unavailable-input exit as an expected silent application failure, and acquires a Fullscreen surface only after input capability preflight while still ordering the surface before any input mode or accepted paint. Reactivating a trapped scope now retains its preferred target until a delayed rendered host attaches, unless a later explicit selection wins. The coding agent assigns approval Enter and Escape to `useFocusScopeInput`, which is the accepted targetless-boundary owner, rather than depending on target attachment timing.

The packed coding-agent, `mo`, `machud`, and fork verifiers pass from the committed artifacts. Image-observed Inline and Fullscreen focus-routing journeys pass modal reopen, target restoration, bracketed paste, normal/alternate buffer ownership, cursor and protocol cleanup, exact termios restoration, and post-exit shell input. Full `vp run ready` and one justified local `CI=true vp run ci` pass at this core lifecycle boundary. No push or GitHub Actions run was triggered.

R2 and R9 closed at the Stage 2 boundary. At that point R3 and conditional R5 still left materially different honest public contracts, so implementation stopped for Yunfei's decision. The accepted 2026-07-15 disposition and local cutover are recorded below.

## Historical Stage 3 checkpoints

Commit `dd14295` repairs R4's decision-independent transaction mechanism without choosing the public entry, name, or collection identity. Static painting now prepares bytes without advancing component state; a normally returned stream write, including `false`, accepts the prepared host identities before any later frame operation; a throwing write abandons the indeterminate batch without automatic retry; and output-free items advance only after the renderer commit succeeds. Acceptance carries the exact shallow item snapshot represented by the prepared render, so an array append made synchronously inside `stdout.write()` remains pending and is emitted by the next commit instead of being skipped by a callback that observes newer collection state.

Durable tests at that checkpoint cover preparation, acceptance, abandonment, callback failure, a red-to-green re-entrant append, visual Inline, screen-reader Inline, final non-TTY, the then-current visual Fullscreen behavior, `write(false)`, accepted Static followed by dynamic failure, throwing Static output, resize, suspension and continuation, coordinated output, and ordinary teardown. Final `vp run ready` passes 693 Runtime tests, 1,558 integration tests, 174 real-PTY tests, the clean Vue 3.4.38 and TypeScript 6.0.3 tarball consumer, types, lint, format, builds, and examples; the required adversarial review passes after catching the re-entrant append loss. No local full CI, push, or GitHub Actions run was added for that checkpoint.

On 2026-07-15 Yunfei accepted the remaining disposition. The public value remains named `Static`; it and exactly five Static-specific named types move from the common root to the supported `@vue-tui/runtime/inline` entry without root aliases or duplicate common exports. The `items`, optional `style`, and Vue scoped-slot `{ item, index }` shape remains. Every committed prefix position is append-only by `Object.is` identity, and remounting begins a new history region. Effective visual Fullscreen rejects component presence, including an empty collection, before Static bytes, dynamic output, observation, or a new frame. A Fullscreen request that resolves to effective Inline through screen-reader fallback remains supported, as do final and live non-TTY streams, deterministic non-Fullscreen models, and string rendering.

The local cutover implements that decision and updates the first-party coding-agent migration patch to checksum `c3d49dd747761e38cd2f6e737d0fa5ffb71a2f9e7d69544f9274957d1f51239b` without changing the immutable Stage 2 checksum record above. At that checkpoint, focused runtime, integration, public value/type, clean-consumer, Fullscreen acquisition/rollback, dynamic insertion, suspension/continuation, re-entrant append/replacement, testing-observer, and real-PTY checks passed; final closure later closed R3 through R5.

R8 was also implemented locally at the precise Box-rooted boundary. Vue runtime-dom reads and writes `el.style.display` before mount without going through the renderer's `patchProp`, so each Box host now exposes one non-enumerable facade that composes two independent facts: the latest authored Box `display` value and whether `v-show` currently hides it. Either `display=none` or `v-show=false` hides; showing again uses the latest authored value. The existing Yoga-derived services then remove the mounted subtree from layout, paint, focus eligibility, visible geometry, caret ownership, and Fullscreen hit testing without separate policy branches. Text is excluded because nested Text uses a Yoga-less virtual host, Transform can resolve through a different host shape, and Static's own `DISPLAY_NONE` is the Inline-history isolation mechanism. At that checkpoint, durable evidence covered the exact runtime-dom failure before the repair, host truth-table and freed-Yoga safety, SFC template typing, deterministic Inline and Fullscreen retained-state/retarget/teardown journeys, a real Fullscreen PTY, and the clean tarball consumer on Vue 3.4.38. Final closure later closed R8. No push or GitHub Actions run was added for this cutover.

R10 was implemented without copying PR #251's unsafe marker representation. The disposable contribution integration passed its original focused and packed gates, then two adversarial cases proved that literal U+E000/U+E001 content was rewritten and a nested reset's suffix inherited the ancestor foreground incorrectly. Runtime now carries foreground-reset boundaries as structural chunks until top-level Text composition, so user strings require no reserved characters and nested `revert` or `initial` spans block only enclosing foreground color. Background and boolean styles still compose, ordinary non-reset Text retains its existing byte path, and a Transform conservatively protects its complete result when its source contains a reset boundary. Focused narrow wrapping, adjacent colored sibling, nested reset, literal private-use, Transform, chalk-level-zero, full Text/background/Transform/render-to-string regression, clean packed Vue 3.4.38 consumption, and final image-observed evidence pass. Commit `1a8ed6b` satisfies the attribution requirement with `Co-authored-by: ahfoysal <ahfoysal30@gmail.com>`.

R6's stable 256-byte-high-water-mark red proof selected and now guards the repair. The Runtime root exports `CoordinatedWriteResult`: `{ status: "accepted", writable: true }` means another transaction may start immediately; `{ status: "accepted", writable: false, ready }` means the complete transaction was handed exactly once and the caller must await flow control; `{ status: "blocked", ready }` means the body did not run and Runtime retained none of the attempted bytes. One application-owned output gate captures whole logical transactions, merges adjacent same-stream segments, preserves stdout/stderr ordering, stops every later physical write until `drain`, and keeps only the latest desired replaceable render frame. Render, Static, geometry, selection, caret, mouse, synchronized output, Fullscreen surface/cursor state, Kitty, paste, and SGR input ownership commit only when their output is physically handed; normal teardown waits for accepted output, its final commit, and ordered restoration, while non-returning emergency teardown remains synchronous best effort. Focused slow-Writable unit and lifecycle tests and the final J6 evidence pass, closing R6.

## Final closure evidence

This section is the authoritative final local evidence. Earlier package hashes, PR snapshots, checkpoint totals, and Stage 1 through Stage 3 statements above remain historical inputs rather than final results.

### Candidate and remote-state boundary

- Implementation head: clean local commit `e31aa1ae0dd69cb46e31416eb02a77ce83e4d491`; the documentation synchronization is the commit containing this section.
- Host: Darwin 25.5.0 arm64 on an Apple M3 Pro with 12 logical CPUs and 36 GiB memory; Node 24.18.0 and V8 13.6.233.17-node.50.
- A read-only GitHub-app query at 2026-07-16 04:44 Singapore time reported PR #265 open, non-draft, unmerged, and mergeable, with base `25df36d8751cde4677a50ec7ceb6c7f6eff97411` and remote head `3d7e197dda71d02a1b8926982a35b1604fcf17e1`. The local implementation head is therefore not the remote PR head.
- No new GitHub Actions run was requested for final closure. Historical run 29332914513 applies to the older remote checkpoint only; it is not evidence that `e31aa1a` ran remotely. No push, PR mutation, merge, release, or publication occurred.

### Packed public consumers

All four verifiers ran serially from the clean implementation head. They used Runtime tarball SHA-256 `8dddda654dc2fb9fe8c4bd5cb59d023a7cd1bfbc70c826a2eddbcf7e28a5effa`; the consumers that needed Vite used SHA-256 `81b5a277c395a4cf179e9c831162c33894e8bbfb237f035235eef6e635d79234`.

| Consumer     | Immutable pin                              | Patch SHA-256                                                      | Final local result                                                                                                  | Log and SHA-256                                                                                                                     |
| ------------ | ------------------------------------------ | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| Coding agent | `3e44c9a266e52ebeba2db669b4bb96521b9e2f3a` | `c3d49dd747761e38cd2f6e737d0fa5ffb71a2f9e7d69544f9274957d1f51239b` | Build and immediate legacy-Escape approval journey pass in a 100x30 real PTY                                        | `/tmp/vue-tui-consumer-coding-agent-final-e31aa1a-20260716.log`, `c69018ef1ea46e7a35c663e657d05bee3b51df5f33be1866c004d67eb131bc0b` |
| mo           | `6bea467a6995f4912e809b417b5c56a3964cc556` | `fa624015de1de3c563d9c95f55332f1c618b2ad238692e9e687bb2b997628751` | Build, tests, global install, accept, cancel, and PTY restoration pass; the pin's 39 warnings remain baseline noise | `/tmp/vue-tui-consumer-mo-final-e31aa1a-20260716.log`, `fdbc89ec6e1f9ddff59a8c1e5724302ff1919e040b6bbfa3e289b7aa640ad2d8`           |
| machud       | `a51a6853686eb818471d0027d2549e6e664c9b36` | `196c404c697712c5c8898a8426cc1a977c7a974d6a881141461c476277b336ce` | Build, tests, canonical verification, one-shot run, and 120x40 to 100x40 real-PTY geometry journey pass             | `/tmp/vue-tui-consumer-machud-final-e31aa1a-20260716.log`, `fa48debafe2d92745cde83614556cd02dc43bc94cb42aed39bc3a151a8bc128b`       |
| Fork/non-TTY | Committed three-process fixtures           | Not applicable                                                     | Both fork topologies, input-free and active-input, repeated three times each, all pass                              | `/tmp/vue-tui-consumer-fork-stdin-final-e31aa1a-20260716.log`, `827126c2dcb50b572c7fba575873f1b6a5e02f2861aeb7bf7f65ed8d53ae9a76`   |

### Capacity and release

The authoritative schema-6 J1-J5 artifact is `/tmp/vue-tui-capacity-j1-j5-final-e31aa1a-20260716.json` with SHA-256 `6b68c3a656596cfb4a511d62a33a5e4658c5ce067cd20fe6dc9bbad2e1324e87`; its command log has SHA-256 `7ceba69decf65593c59d9cdb394047b9aa89feb822208547c547cd5f652cc685`. It records implementation commit `e31aa1a` with `dirty=false`, three warmups, and ten measured repetitions per journey. Every journey passed on its first formal round with no hard failure, repeat reason, or maintainer-decision reason.

| Journey | Input latency p95 / maximum ms | Heartbeat p99 / maximum excess ms | Render p99 / maximum ms |
| ------- | ------------------------------ | --------------------------------- | ----------------------- |
| J1      | Not applicable                 | 2.8400 / 39.8953                  | 1.6613 / 8.6485         |
| J2      | 42.6976 / 45.1102              | 4.2682 / 5.3236                   | 3.3445 / 5.0372         |
| J3      | 50.4527 / 91.5648              | 9.9180 / 23.6066                  | 5.4169 / 168.2031       |
| J4      | 73.0023 / 109.4647             | 19.0700 / 62.2490                 | 7.8115 / 20.3351        |
| J5      | 45.5459 / 50.2904              | 5.2792 / 20.5542                  | 6.1635 / 12.3180        |

Render duration is diagnostic and has no independent gate; in particular, J3's 168.2031 ms maximum is not a hidden failure. Runtime resource counters returned exactly to zero, every Yoga node was freed, retention calibration and census coverage passed, and all ten tracked target kinds had zero surviving witnesses after every warmup and measured repetition. The current-context memory estimator recorded small positive deltas of 54,216, 39,944, 82,824, 33,352, and 42,240 bytes for J1 through J5; those samples are explicitly diagnostic and do not claim whole-process leak freedom or affect acceptance.

The authoritative schema-6 J6 artifact is `/tmp/vue-tui-capacity-j6-final-e31aa1a-20260716.json` with SHA-256 `1fa3a1bb669da9bff9281aaa930a16094e3a1a42d7de629110ba33af2ea43b9b`; its command log has SHA-256 `d542e5159416e5ce5f5ca5d9168abd24e232b5570c364b41bd35c47ea928bc5e`. It also records implementation commit `e31aa1a` with `dirty=false`; five repetitions at each volume passed.

| Workload   | `write(false)` / `drain` | Writes before `drain` | Largest atomic / maximum queued bytes | Coordinated attempts             | Final release evidence                                                       |
| ---------- | ------------------------ | --------------------- | ------------------------------------- | -------------------------------- | ---------------------------------------------------------------------------- |
| J6-I small | 28-29 / 28-29            | 0                     | 3,771 / 3,779                         | 20 backpressured; 6-7 blocked    | Pending state returned to the declared bound; Yoga 106 created / 106 freed   |
| J6-I large | 311-314 / 311-314        | 0                     | 4,252-4,363 / 4,260-4,371             | 200 backpressured; 63-65 blocked | Pending state returned to the declared bound; Yoga 1,052-1,054 / 1,052-1,054 |
| J6-F small | 86 / 86                  | 0                     | 5,973 / 5,981                         | 20 backpressured and blocked     | Pending state returned to the declared bound; Yoga 1,565 / 1,565             |
| J6-F large | 866 / 866                | 0                     | 5,975 / 5,983                         | 200 backpressured and blocked    | Pending state returned to the declared bound; Yoga 1,565 / 1,565             |

All J6 repetitions preserved exact output, latest-frame visibility, backpressure ordering, retention release, Runtime resource release, and Yoga release. J6 recorded five diagnostic memory samples per workload and control, below the ten-sample diagnostic threshold, so no aggregate-memory conclusion is drawn; memory does not affect schema-6 acceptance.

### Focus, PTY, and visual evidence

The final focus correction preserves an accepted generation only for host-only rendered reconciliation when no logical mutation is pending and the complete semantic input is unchanged: route boundary, owner, ordered ancestors, and external owner are identity-equal, and both the full rendered-target sequence and the eligible sequential-target sequence are identity- and order-equal. Any logical mutation or route/traversal change publishes a replacement generation; reuse preserves the existing demand lease. F3 remains fail-closed only for the stale selected path captured by that fact, while independently captured application-global handlers for the same fact remain unaffected. Tests cover semantic no-op reuse, sequential and non-sequential owner reorder, visibility change, deactivation, a reactivated approval boundary receiving a lone legacy Escape in Inline and Fullscreen, and the matching real-PTY approval journey.

Final repository preparation exposed and repaired a separate Fullscreen row-diff regression. The outer render transaction had already applied the candidate caret declaration before the byte-identical-frame guard compared it, so that guard could mistake an uncommitted physical caret move, show, or hide for a no-op. Commit `52fadce` now permits the skip only when the writer's physical cursor state is also clean; the testing terminal covers a visible move from column one to column two and the later visible-to-inactive transition. Commit `e31aa1a` keeps capacity lifetime census strict by default while letting the unrelated focused row-diff test opt out explicitly. The affected real-terminal visual journeys were rerun after both repairs.

Four final controller-driven terminal journeys were observed one action at a time and their important PNGs were loaded for visual inspection:

| Journey                         | Artifact directory                                                             | Observed result                                                                                                                                                                             |
| ------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Fullscreen foreground reset     | `tui-visual-review-results/fullscreen-origin-session-2026-07-15T19-44-47-112Z` | Nested `revert`/`initial`, inherited blue background, narrow wrapping, sibling colors, literal U+E000/U+E001, and shell restoration are correct                                             |
| Inline history                  | `tui-visual-review-results/inline-history-session-2026-07-15T19-45-32-354Z`    | `DONE 0`, `DONE 1`, and `DONE 2` appear once in committed history with newest `TAIL 2`; shell history remains usable                                                                        |
| Box-rooted `v-show`             | `tui-visual-review-results/v-show-session-2026-07-15T20-45-52-377Z`            | Hide, hidden update, show-again as `probe:2`, mouse/caret release and reacquisition, one mount, and one exit-time unmount are correct                                                       |
| Fullscreen selection and OSC 52 | `tui-visual-review-results/selection-copy-session-2026-07-15T20-47-39-316Z`    | Keyboard selection, requested copy status, clear, and mouse selection of complete `你🙂 beta gamma` graphemes across a soft wrap are correct; structured cells mark only that range inverse |

Every visual artifact set records application exit code 0, exact termios restoration, no controller errors, normal-buffer return, disabled application-owned modes, and successful shell input after application exit. The exit, error, and termios facts are in `process.json`; final buffer and mode state is in the structured screen records, and the post-exit shell action is in `actions.jsonl` and its matching screen. OSC 52 evidence proves that Runtime requested transport with exact text; it does not claim that an external clipboard accepted the request.

### Final repository and review gates

- Final local repository gate: one uncached local `vp run ready` completed 27 of 27 tasks successfully on the synchronized record: formatting, zero-warning lint, types, package and example builds, the clean Vue 3.4.38 and TypeScript 6.0.3 tarball consumer, 746 Runtime tests, 89 testing tests, 31 component tests, 30 Vite tests, 1,604 passing integration tests plus two expected failures and two skips, 176 real-PTY tests, and six example tests. The log is `/tmp/vue-tui-ready-final-e31aa1a-20260716.log` with SHA-256 `f8bb86ab3e792e74c0be6fff13cc3e681348f4c5da7b6f2f8092efb05171d89a`.
- Final adversarial review: one fresh read-only round found two record inconsistencies—the superseded heap-growth rule still appeared current, and visual facts were attributed only to `process.json`—but no implementation defect. Both were corrected. The required final round with the same reviewer confirmed those repairs and found one remaining low-severity J6 memory-sample wording error; that wording was corrected from the schema-6 artifact before this commit. No review ran tests, CI, GitHub Actions, Docker, or external mutation.
