# TODOs

Concrete follow-up work that Yunfei explicitly asked the project not to forget. This file does not decide public API direction and is not a source of speculative work. Public API authority remains the [Runtime public API decision ledger](./runtime-public-api-decisions.md); complete or remove an item when the implementation and its focused evidence land.

## Runtime public API review

- [x] Internalize the official Runtime tooling bridges without changing their behavior: remove `@vue-tui/runtime/devtools` and `@vue-tui/runtime/testing` from the supported public contract; move `connectDevtools`, `createTestHostBridge`, and the bridge-specific test types behind narrowly named internal package entries used by the version-coupled `@vue-tui/vite` and `@vue-tui/testing` packages; update those imports, package exports and tarball contents, exact public and internal boundary guards, clean consumers, documentation, and focused tooling tests. Do not publish a broad internal barrel, grant application-facing higher layers privileged access, or claim third-party replacement support. This work is complete: official Vite and testing packages use the privileged internal entries, and those bridges are not a third-party public Runtime contract.
- [x] Implement the vouched Box measurement contract: replace `useBoxSize()` with `useBoxMetrics()` and `UseBoxMetricsReturn`; publish readonly `width`, `height`, parent-relative `left`, parent-relative `top`, and `hasMeasured` refs from one coherent accepted-measurement state; keep the direct current-app Box target; remove the experimental name without an alias; keep `measureElement()` absent; migrate tests, package guards, README, changelog, and the Runtime audit documents.
- [x] Implement the vouched root layout-size contract: replace experimental `useLayoutWidth()` and `useViewportHeight()` with `useLayoutSize()` and `UseLayoutSizeReturn`; expose readonly reactive `width` and `height` refs from one accepted dimension snapshot; use `width` and `height` consistently across the public author-facing layout surface; remove the old hooks and the considered `useViewportSize()` without aliases; keep physical terminal `columns` and `rows` private. Change `RenderToStringOptions` to `width?: number` and `height?: number`, default to 80×24, accept only positive bounded integers except for explicit positive `Infinity` height, map `Infinity` to the internal unbounded representation rather than Yoga, bound finite ordinary dynamic layout without padding shorter output, give the mounted non-TTY document host the same fixed modeled 80×24 layout without resize, require complete-document callers to select `height: Infinity`, and update focused host, resize, string, type, clean-consumer, README, changelog, review, and package-boundary evidence.
- [x] Implement the vouched supported mounted-document host without inventing another public mode or sizing API: keep mounted live-TTY Inline and Fullscreen as the primary application hosts; stop throwing solely because a Fullscreen request has non-TTY stdout; select the same non-interactive document path for Inline and Fullscreen non-TTY mounts; emit no terminal screen-management controls or intermediate dynamic frames; write accepted Static and coordinated console output when committed; on clean teardown write the current non-empty dynamic document once with a line ending only when missing; write no bytes for empty dynamic output; do not replay a stale successful document on error; keep `useInput()` inert; make `useStdin()` return the exact mounted stream for caller-owned observation while reporting no raw support and never changing raw state even for a TTY stdin; preserve mounted Vue lifecycle, app barriers, stream borrowing, used-stream failures, rollback, and cleanup; update focused host, input, output, lifecycle, clean-consumer, README, changelog, review, and package-boundary evidence. Treat this as a supported secondary contract paired with explicit `renderToString()`, not as undefined behavior or a primary interactive host.

## Deferred higher-layer work

- [ ] Revisit an explicit, optional Ink-like error boundary and formatted error screen in `@vue-tui/components` after the Runtime foundation is settled. It must use only Vue and public Runtime APIs, must not become a hidden Runtime wrapper or mount option, and must not assume automatic exit, retry, source-file access, or Fullscreen error durability until real application use establishes those contracts.
